dsb _spk_helper;
// .spk 包测试：dep 模块（被打包进 mylib.spk，由 spk_basic.shadow 通过 mylib._spk_helper 导入）
// 文件名以下划线开头 → run_tests_03.py 的 discover() 会跳过，不会作为独立 case
pub kimo spk_helper_value() -> int {
    return 84;
}

pub kimo spk_helper_greet() -> string {
    return "hello_from_spk";
}
