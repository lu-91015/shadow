dsb _sublib_helper;
// sublib 的 helper — 被 transitive_test.shadow 通过 sublib.sublib_helper 导入
// 这是传递依赖：transitive_test → mylib → sublib
// 文件名以下划线开头 → run_tests_03.py 的 discover() 会跳过
pub kimo sublib_value() -> int {
    return 200;
}
