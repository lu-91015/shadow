dsb _helper;
// 阶段6 测试：dep 模块（被 sbg_basic.shadow 通过 mylib._helper 导入）
// 文件名以下划线开头 → run_tests_03.py 的 discover() 会跳过，不会作为独立 case
pub kimo sbg_helper_value() -> int {
    return 42;
}

pub kimo sbg_helper_greet() -> string {
    return "hello_from_dep";
}
