//@mode: tc-fail
//@desc: LSP-1 parser error recovery - one parse pass reports BOTH reserved-keyword errors (second error message must appear)
//@expect-stderr: 'yield' is a reserved keyword
dsb parse_recover_multi_errors;

kimo first() -> int {
    let do = 1;
    return 0;
}

kimo second() -> int {
    let yield = 2;
    return 0;
}

kimo main() -> int {
    return 0;
}
