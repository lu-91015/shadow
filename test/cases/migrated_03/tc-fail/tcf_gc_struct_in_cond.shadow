//@desc: use struct field of wrong type in condition should fail
//@expect-stderr: Type check failed
dsb tcf_gc_struct_in_cond;
struct P { x: int, y: int }
kimo main() -> int {
    let p = P { x: 1, y: 2 };
    if (p) {
        return 1;
    }
    return 0;
}
