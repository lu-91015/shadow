//@desc: string subscript s[i] is typed as string and comparable to a literal
//@expect: 1
dsb tc_string_subscript;
kimo main() -> int {
    let s = "hello";
    let ch = s[0];
    if (ch == "h") { println(1); return 0; }
    if (ch != "z") { println(2); return 0; }
    println(0); return 0;
}
