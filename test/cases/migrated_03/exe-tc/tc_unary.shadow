//@desc: unary minus and logical-not type-check
//@expect: 0
dsb tc_unary;
kimo f(x: int) -> int {
    let y = -x;
    let z = (x > 0);
    if (!z) { return y; }
    return 0;
}
kimo main() -> int { println(f(5)); return 0; }
