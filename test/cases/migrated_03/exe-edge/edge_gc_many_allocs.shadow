//@desc: GC many struct allocations in loop
//@expect: 0
dsb edge_gc_many_allocs;
struct P { x: int, y: int }
kimo main() -> int {
    let i = 0;
    while (i < 50) {
        let p = P { x: i, y: i + 1 };
        i = i + 1;
    }
    println(0); return 0;
}
