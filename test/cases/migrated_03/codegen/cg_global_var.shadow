//@ mode: codegen
//@ desc: top-level mutable variable
dsb cg_global_var;
mut counter: int = 0;
kimo inc() -> int {
    counter = counter + 1;
    return counter;
}
kimo main() -> int {
    return inc() + inc();
}
