//@desc: GC frame anchor (compile-time stack map) push/pop even with no GC objects
//@expect-ir: shadow_gc_tls_frame_head
dsb gc_10_plain_fn_frame;
kimo plain(x: int) -> int { return x + 1; }
kimo main() -> int {
    return plain(42);
}
