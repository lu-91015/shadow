//@desc: GC with struct field chain in expression
//@expect-ir: shadow_gc_alloc
dsb gc_19_field_chain;
struct P { x: int, y: int }
kimo main() -> int {
    let p = P { x: 10, y: 20 };
    let a = p.x;
    let b = p.y;
    let c = a + b;
    let d = c - p.x;
    return d;
}
