//@desc: GC alloc inside if branch
//@expect-ir: shadow_gc_alloc
dsb gc_11_if_branch;
struct P { x: int, y: int }
kimo main() -> int {
    let flag = 1;
    if (flag > 0) {
        let p = P { x: 1, y: 2 };
        return p.x;
    }
    return 0;
}
