//@ mode: codegen
//@ desc: method call obj.method(args) desugars to method(obj, args)
//@expect-ir: call
dsb cg_method_call;

struct Point { x: int, y: int }
kimo get_x(p: Point) -> int { return p.x; }
kimo main() -> int {
    let p = Point { x: 42, y: 99 };
    return p.get_x();
}
