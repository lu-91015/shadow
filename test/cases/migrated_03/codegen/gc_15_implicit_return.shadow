//@desc: GC frame anchor pop on implicit fall-through return (no explicit return)
//@expect-ir: shadow_gc_thread_frame_head
dsb gc_15_implicit_return;
struct P { x: int, y: int }
kimo helper(p: int) -> int {
    let q = P { x: p, y: 0 };
}
kimo main() -> int {
    return helper(5);
}
