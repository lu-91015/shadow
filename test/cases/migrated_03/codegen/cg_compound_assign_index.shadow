//@ mode: codegen
//@ desc: compound assignment on array element (arr[i] += 1) desugars to arr[i] = arr[i] + 1
//@expect-ir: load
dsb cg_compound_assign_index;

kimo main() -> int {
    let arr = [10, 20, 30];
    arr[0] += 5;
    arr[1] *= 2;
    return arr[0];
}
