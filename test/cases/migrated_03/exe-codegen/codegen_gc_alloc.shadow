//@desc: GC alloc + root management for struct on GC heap
//@expect: 10
dsb codegen_gc_alloc;
struct Point { x: int, y: int }
kimo main() -> int {
    let p = Point { x: 10, y: 20 };
    println(p.x); return 0;
}
