//@desc: unary minus codegens to a sub from zero
//@expect: 1
dsb codegen_unary;
kimo f(x: int) -> int { let y = -x; if (y < 0) { return 1; } return 0; }
kimo main() -> int { println(f(5)); return 0; }
