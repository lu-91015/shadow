//@expect: 7
//@ desc: generic monomorphization — first returns the generic T value
dsb cg_mono_return;

kimo first<T>(a: T, b: T) -> T {
    return a;
}
kimo main() -> int {
    let v = first(7, 99);
    println(v); return 0;
}
