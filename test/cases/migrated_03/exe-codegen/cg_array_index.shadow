//@desc: array index access codegens to shadow_array_get call
//@expect: 30
dsb cg_array_index;
kimo main() -> int {
    let arr = [10, 20, 30];
    let x = arr[0];
    let y = arr[1];
    println(x + y); return 0;
}
