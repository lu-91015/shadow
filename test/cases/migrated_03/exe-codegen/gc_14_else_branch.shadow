//@desc: GC alloc in else branch
//@expect: 2
dsb gc_14_else_branch;
struct P { x: int, y: int }
kimo main() -> int {
    let flag = 0;
    if (flag > 0) {
        println(0); return 0;
    } else {
        let p = P { x: 1, y: 2 };
        println(p.y); return 0;
    }
}
