//@desc: logical && || ! type-check with bool operands
//@expect: typecheck: 1
dsb tc_logical;
kimo f(a: int, b: int) -> int {
    let r = (a > 0) && (b > 0);
    let s = (a > 0) || (b > 0);
    if (r && s) { return 1; }
    return 0;
}
kimo main() -> int { return f(1, 0); }
