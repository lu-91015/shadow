dsb _mod_a_helper;
// Helper module A — defines add and a pub struct Pair
pub struct Pair { x: int; y: int }
pub kimo add(a: int, b: int) -> int { return a + b; }
pub kimo make_pair(x: int, y: int) -> Pair { return Pair { x: x, y: y }; }
