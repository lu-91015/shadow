//@desc: mutable binding reassignment type-checks
//@expect: typecheck: 1
dsb tc_assignment;
kimo f() -> int {
    let x = 0;
    x = x + 1;
    x = x * 2;
    x = x - 3;
    return x;
}
kimo main() -> int { return f(); }
