dsb _mod_b_helper;
// Helper module B — also defines a `add` function with different behavior
pub kimo add(a: int, b: int) -> int { return a * b; }
pub kimo mul(a: int, b: int) -> int { return a * b; }
