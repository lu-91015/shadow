//@desc: struct field in if condition
dsb tc_gc_struct_field_cond;
struct P { x: int, y: int }
kimo main() -> int {
    let p = P { x: 10, y: 5 };
    if (p.x > p.y) {
        return 1;
    }
    return 0;
}
