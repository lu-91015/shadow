//@mode: exe
//@desc: 修复回归 — 嵌套动态数组裸 any 元素直接打印。动态数组元素为裸 any（未装箱，值语义），
//       println(c[0][0]) 会把裸整值当作 AnyBox 指针解引用 → AV 静默崩溃。修复后直接输出元素值 42。正式回归用例。
//@expect: 42
dsb ex_dyn_elem_print;

kimo main() -> int {
    let c = [];
    c = array_push(c, []);
    c[0] = array_push(c[0], 42);
    println(c[0][0]);
    return 0;
}