//@mode: exe
//@desc: 整型右移 >> 增量能力。当前编译器对 >> 无整型移位语义，需实现整数右移。
//       16 >> 2 = 4。仅作守门：实现后移除 //@xfail。
//@xfail: 右移未实现：let y = x >> 2 无整数右移语义。修复后应输出 4。
//@expect: 4
dsb ex_shr;

kimo main() -> int {
    let x = 16;
    let y = x >> 2; // 16 >> 2 = 4
    println(y);
    return 0;
}