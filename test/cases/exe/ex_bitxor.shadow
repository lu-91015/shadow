//@mode: exe
//@desc: 位异或运算 ^ 增量能力。整数位异或语义。6(110) ^ 3(011) = 5(101)。正式回归用例。
//@expect: 5
dsb ex_bitxor;

kimo main() -> int {
    let x = 6; // 110
    let y = x ^ 3; // 110 ^ 011 = 101 = 5
    println(y);
    return 0;
}