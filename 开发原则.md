# Shadow 0.5 开发原则

> 本文档定义 Shadow 0.5 编译器开发的核心原则、完整功能清单、自举方案、GC 系统设计
> 与开发步骤。所有代码改动必须遵循本文件中的约定。
>
> 版本：v3.0 ｜ 当前状态：shadow-0.5 严格态唯一路径已收口，自举 fixed-point 达成
> （`stage1.ll == stage2.ll == stage3.ll`，11,031,861 B；`build/shadow.exe` 1,754,112 B；
> 全量回归 927 用例；LSP e2e 5/5）。历史起点为 shadow-0.4 commit 58b8477 的全绿快照。

---

## 0. 项目定位

| 版本 | 实现语言 | 定位 | 关键特性 | 自举状态 |
|------|----------|------|----------|----------|
| 0.1 (shadow-cpp) | C++ | 引导编译器 | 解释器 + LLVM 后端 + 完整类型系统 | — |
| 0.2 (shadow-lang) | Shadow | 编译器 | import 模块系统；依赖 C++ 运行时 | ❌ 不能自举 |
| 0.3 (shadow-0.3) | Shadow | 编译器 | HIR/MIR 管道 + LSP 服务器 + SBG/SPK + 分代 STW GC；由 0.2 引导编译为 `s03.exe` | ❌ 不能自举 |
| 0.4 (shadow-0.4) | Shadow | 自举编译器 | 功能/语法完全复刻 0.3 + 真自举 + Go 级 GC + 纯系统调用运行时；兼容态/严格态双轨 | ✅ 自举（929/931 全绿） |
| **0.5 (shadow-0.5)** | **Shadow** | **自举编译器（严格态唯一）** | **删除兼容态（物理拼接语义）；严格态包引入（Java 模型 import）为唯一路径；编译器自身源码即严格态组织** | ✅ **已自举**（fixed-point 11,031,861 B） |

**一句话定位**：shadow-0.5 在 shadow-0.4 全绿快照（commit 58b8477）之上独立演进，
**删除兼容态**（`tc_check_program` 物理拼接、`ctx.strict==0` 分支、`--strict` 收敛为默认严格态），
**严格态包引入（§3.8：点号=目录、pub 可见性、循环 import 放开、SPK 依赖）成为唯一路径**；
最终产物为单一可执行文件 `shadow`，能够编译自身并达成 `stage2.ll == stage3.ll` 的
固定点（fixed-point）自举验收。

### 0.1 目录布局

```
shadow-0.5/                  # 独立 git 仓库，与 shadow-0.4 平级
├── 开发原则.md              # 本文档
├── build_shadow.sh          # 一键自举脚本（唯一入口，见 §4）
├── bootstrap/               # 链接期冻结工件（见 §4.2）
│   ├── runtime_for_selfhost.o   # 宿主运行时（参与 shadow.exe 链接）
│   ├── miniz.o / miniz.h        # SPK ZIP 支持
│   ├── runtime_clang.o          # 历史遗留，当前链接行未使用
│   └── s03.exe                  # 历史遗留，0.5 引导链已不使用（见 §4.1）
├── build/                   # 构建产物（stage*.ll、shadow.exe、rt/*.o、build.log）
├── src/                     # 0.5 编译器源码（严格态 import 组织）
│   ├── main.shadow          # 入口：管线调度 + CLI + 模块加载 + .lu 缓存门控
│   ├── core/                # AST / Type / Location / 常量 / 工具
│   ├── lexer/               # 词法分析
│   ├── parser/              # 语法分析（Pratt / Precedence Climbing）
│   ├── type_checker/        # 类型检查（含泛型单态化；严格态唯一）
│   ├── hir/                 # HIR 定义与降级
│   ├── mir/                 # MIR 定义与降级（+ .lu 序列化）
│   ├── codegen/             # LLVM-C API 代码生成
│   ├── runtime/             # 运行时库（Shadow 层）
│   ├── gc/                  # GC 运行时（对标 Go，见 §5）
│   ├── sbg/                 # 构建系统（shadow.sbg / shadow.lock）
│   ├── semver/              # 语义版本解析
│   ├── spk/                 # .spk 打包/解包
│   ├── std/                 # 标准库 Shadow 源码
│   ├── lsp/                 # LSP 语言服务器（117 项功能）
│   └── api/                 # 编译器即库 API（compile_api）
├── rt/                      # C 运行时（仅系统调用层，见 §6）
│   ├── rt_core.c            # 字符串/数组/any 盒/哈希等核心原语
│   ├── rt_err.c             # panic / 错误路径
│   ├── rt_extra.c           # 杂项内建
│   ├── rt_fs.c              # 文件与目录 I/O
│   ├── rt_io.c              # 标准输入输出（仅供 tools/link_rt.py 链用户程序，见 §6.1）
│   ├── rt_gc.c              # GC 主体（三色标记 + 写屏障），链用户程序时使用
│   ├── rt_gc_stub.c         # GC 桩（当前构建链未编译，备用）
│   ├── rt_proc.c            # 进程调用（llc / clang）
│   ├── rt_thread.c          # 线程与同步（spawn / await）
│   ├── rt_time.c            # 时间类型支持
│   ├── rt_zip.c             # ZIP STORE 容器（SPK）
│   └── shadow_gc_supplement.c   # GC 补充符号（参与 shadow.exe 链接）
├── test/                    # 完整测试套件（见 §8）
│   ├── run_tests.py         # 测试运行器（-j 并行 + 自动清缓存 + 自动重试）
│   ├── test_lsp.py          # LSP e2e 测试（JSON-RPC 帧级）
│   ├── cases/               # 用例：0.5 新增用例 + migrated_03/（迁移自 0.3）
│   ├── compat_removed_05/   # 删除兼容态时下线的用例（仅存档，不参与回归）
│   └── tools/               # 测试辅助脚本
└── tools/                   # 构建/校验工具（verify_fixed_point.py、link_rt.py 等）
```

---

## 1. 九条核心开发原则

以下九条原则由用户明确指定，具有最高优先级，任何实现决策不得与之冲突。

### 原则 1：在 0.4 全绿基线上演进，删除兼容态，严格态包引入为唯一路径

- **0.4 已达成**：shadow-0.3 的全部功能（语言特性、编译管线、运行时库、工具链 CLI、
  SBG/SPK/semver、LSP 服务器 117 项功能）已在 0.4 完整实现，全量测试 929/931 全绿。
- **0.5 的演进核心**（用户明确要求）：
  1. **删除兼容态**：`tc_check_program`（物理拼接语义）及其全部 `ctx.strict==0` 分支、
     `--strict` flag（收敛为默认严格态），消除双轨。
  2. **严格态包引入为唯一路径**：§3.8 的 Java 模型 import（点号=目录、pub 可见性、
     循环 import 放开、SPK 依赖）成为唯一机制，编译器自身源码即按严格态组织。
  3. 附带修复 0.4 已知缺口：跨模块泛型调用缓存判定（AST 级）、LSP hover 符号名回显。
- 功能清单见 §3，作为核对基准。每完成一项，在 §3 清单中打勾并关联迁移测试。

### 原则 2：shadow-0.5 必须可以自举

- shadow-0.3 不可自举（`s03.exe` 由 shadow-lang 编译）。shadow-0.5 的最终目标是以自身
  编译自身，达成 fixed-point 验收：`stage2.ll == stage3.ll`（逐字节相等）。
- 自举链与验收标准见 §4。**自举是发布门槛，不是可选项。**
- 自举验收必须接入 CI：每次源码变更后自动重放 stage1 → stage2 → stage3 并比对。

### 原则 3：运行时尽量不引入外部操作，仅保留系统调用（参考 Rust）

- 参考 Rust 的哲学：运行时（runtime）只建立在**直接系统调用**之上，不引入任何外部库依赖。
- **编译器本体（`shadow` 可执行文件）依赖 `LLVM-C.dll`（LLVM-C API）——这是生成 `.ll` 文件
  的必要工具链依赖，必须保留**（0.5 链接行显式带 `-lLLVM-C`，见 §4.3）。
- 编译器**产物程序**（用户编译出的 exe）的运行时依赖面必须为：
  - 仅操作系统 API（Windows：`kernel32.dll`/`ntdll.dll` 等系统 DLL；Linux/macOS：libc syscall 封装层）；
  - **禁止**依赖 C++ 运行时库、STL、`miniz` 等第三方代码进入产物程序；
  - 产物程序**不链接 `LLVM-C.dll`**：LLVM-C 只服务于编译器的 `.ll` 生成，产物程序不需要。
- 内存分配、字符串、数组、哈希表、锁、线程、文件 I/O、时间、进程调用等全部运行时能力
  必须由 0.4 自研的 `rt/` 系统调用层提供（见 §6）。
- 验收：`shadow` 本体依赖 LLVM-C.dll（工具链依赖，允许）；`--exe` 产物用
  `dumpbin /dependents`（Windows）或 `ldd`（Linux）检查，依赖列表必须仅为系统 DLL/库；
  新增任何第三方依赖都需要文档说明与用户确认。

### 原则 4：打包后的可执行文件名为 `shadow`

- 所有平台的编译产物统一命名为 `shadow`（Windows 下为 `shadow.exe`，Linux/macOS 为 `shadow`）。
- 不再使用 `s03.exe` / `shadow-lang.exe` 等旧命名。
- LSP 服务器、SPK 工具、编译器三合一，全部通过 `shadow` 的子命令/参数入口暴露（见 §3.4）。
- VSCode 扩展等配套工具的 `compilerPath` 配置指向 `shadow`。

### 原则 5：将 shadow-0.3 的测试用例完全迁移

- shadow-0.3 的全部测试（历史约 929 个 `.shadow` 用例 + 31 个 `.lu` + 32 个 `.sig` + 各辅助文件）
  必须**逐一迁移**到 shadow-0.5 的 `test/cases/`，语义不丢、断言不丢。
  （0.5 当前 `run_tests.py --list` 实收 **927** 个用例，见 §8.2；与 0.3 原数的微小差异来自个别用例未纳入运行器发现逻辑。）
- 迁移是**复刻**：0.5 语法与 0.3 完全一致（原则 6），用例可直接迁移，仅需适配 0.5 工具
  的路径/命名（如可执行文件 `shadow`）；`//@mode/desc/expect/expect-stderr/skip/xfail/pos/timeout`
  指令语义保持一致。
- 测试目录结构与 0.3 一一对应（见 §8.2 目录分布；0.3 用例整体置于 `test/cases/migrated_03/`），运行器 `test/run_tests.py` 兼容
  0.3 的三种断言形态（rc 判定、.ll 非空、exe stdout 比对 + 查询类 stdout 子串）。
- 验收门槛：**迁移测试全绿（PASS 数 ≥ 0.3 基线，0 FAIL、0 CRASH）** 是每个功能合入的前提。

### 原则 6：语法完全复刻 shadow-0.3（包括 `kimo`、`dsb` 等全部语法特性）

- shadow-0.5 的**语言语法完全复刻 shadow-0.3**：`dsb <name>;` 模块声明、`kimo fn`、
  `import a.b.c`、`pub` 体系、`any` 盒子、`@extern`、`with`/`defer`/`?`/元组/字符串插值等
  全部语法特性，一个都不少。
- 0.5 编译器源码使用与 0.3 一致的语法体系书写（`kimo`、`dsb` 照常使用）；代码组织与
  模块边界可按 0.5 自身工程需要组织，但**语法层面必须完整复刻**。
- **语法复刻的收益**：0.5 源码天然是 0.3 语言的合法子集，由 0.5 自举链
  （`build/shadow.exe` 冷启动宿主，见 §4.1）逐级编译，自举链无障碍成立。
- 语法复刻之外若需新增特性（超出 0.3 能力），遵循"**先用 0.3 可编译的旧写法实现 →
  stage1 支持 → stage2 才在源码中使用**"的演进顺序（见 §4.1）。
- 验收：0.5 源码可由 `build/shadow.exe` 完整编译（语法层面零差异）；`kimo`/`dsb`/
  `import`/`pub` 等语法特性在 0.5 测试用例中全部可用。

### 原则 7：完善 GC 系统，完全对标 Go 的 GC 系统

- shadow-0.3 的 GC 为**分代 STW mark-sweep + 保守式栈/堆扫描**，与 Go GC 差距巨大
  （无并发标记、无写屏障、无自动触发、依赖手工 root API、高停顿）。
- shadow-0.5 的 GC 必须**完全对标 Go GC** 的架构与行为：
  - 并发（concurrent）三色标记，非分代、非压缩；
  - 编译器自动插入写屏障（Go 1.8 混合写屏障：插入 + 删除）；
  - 精确根集：编译器为每个函数生成 stack map / register map，全局变量 map；
  - GC 辅助（GC assist）：分配时按堆增长比例协助标记；
  - 惰性清扫 + 后台并发清扫；堆增长触发（GOGC 模型）+ 最小阈值；
  - 目标：标记阶段基本不 STW，仅极短的标记终止停顿；
  - 与 0.4 的多线程（spawn/await 协程）模型整合，支持多线程并行标记。
- 完整设计见 §5，分阶段实现见 §9 Phase 8-9。
- 验收：GC 压测用例（大量分配 + 并发协程 + 指针图遍历）内存峰值可控、无泄漏、
  长驻进程（LSP）内存稳定。

### 原则 8：不要省略实现，务实实现，需要完整方案

- **禁止**"跳过实现"：任何列出的功能必须给出真实实现，不允许占位、空壳、TODO 敷衍。
- **禁止**跳过管线阶段直接生成 LLVM IR（0.3 已确立 HIR/MIR 不可省略，0.4 沿用）。
- 每个阶段必须可编译、可运行、有测试，不允许长期断链（渐进式交付）。
- 本文件 §3 功能清单 + §9 阶段计划共同构成完整方案；实现过程中如发现新功能缺口，
  必须补入清单与计划，不允许静默丢弃。

### 原则 9：字符串拼接时必须注意内存问题

- shadow-0.3 的 ABI 文档已明确承认："编译器内部大量字符串拼接仍可能造成内存膨胀"，
  且字符串为 NUL 结尾 C 串，链式 `+` 拼接为 O(n²) 反复分配拷贝。
- shadow-0.5 必须系统性解决字符串内存问题（完整设计见 §7）：
  1. 字符串表示改为**长度前缀 + 缓冲区**（不再以 NUL 结尾为唯一事实来源）；
  2. 提供 `str_join` 高性能拼接：预计算总长度、一次性分配结果缓冲、逐段拷贝，
     从根上消除链式拼接的反复分配（编译器内部统一使用，见 §7）；
  3. 编译器**常量折叠**：编译期字符串字面量拼接折叠为单个字面量；
  4. 运行时 `+=` / `+` 链式拼接：唯一引用时可原地扩展（参考 Rust `String::push_str`）；
  5. 字符串字面量 **intern pool** 去重；
  6. 编译器内部（lexer/parser/type_checker/codegen）统一使用 `str_join` 拼接，禁止裸 `+` 链；
  7. 长驻进程（LSP）与自举编译器必须配合 GC，避免字符串膨胀导致 OOM。
- 验收：10 万次拼接压测（`//@desc: stress concat` 用例）通过且内存峰值随拼接次数
  近似线性；编译器自身编译大型源码（如自举源码）内存可控。

---

## 2. 总体架构原则

### 2.1 编译管道（完整实现，不跳过）

```
Shadow 0.4 源码
    │  Lexer
    ▼
  Tokens
    │  Parser（Pratt / Precedence Climbing）
    ▼
  AST ── Expr / Stmt / Program / FuncDef / StructDef / EnumDef / TraitDef ...
    │  TypeChecker（含泛型单态化、约束求解）
    ▼
  带类型 AST（inferred_type 全部 concrete）
    │  lower_to_hir
    ▼
  HIR ── 去糖（if/match/for/复合赋值/短路/字符串插值展开）、名字→slot 解析
    │  lower_to_mir
    ▼
  MIR ── 线性指令 + 显式基本块（MirInst + MirBlock + MirFunc，29+ 种 MIR_OP）
    │  LLVM-C API（cg_gen_program）
    ▼
LLVM Module（由 LLVM-C API 构建，禁止字符串拼接 IR）
    │  LLVMPrintModuleToString
    ▼
  .ll 文本 → llc → .o → 链接（自研运行时 .o + 系统库）→ shadow / 用户 exe
```

### 2.2 核心不变量

1. **对外产物仍是 `.ll` 文本**：下游 `llc` / 链接器 / 运行时 ABI 保持稳定。
2. **类型安全优先**：类型错误必须在编译期捕获，不静默降级为运行时行为。
3. **渐进式自举**：每一步都必须可工作、可测试，不允许长期断链。
4. **LLVM-C API 构建 Module**：禁止字符串拼接 IR。
5. **编译器即库**：编译管线必须以库形式暴露（`compile_api`），支撑 LSP 与 CLI 复用。
6. **诊断质量**：所有错误带 `file:line:col` 与稳定错误码（沿用 0.3 的 LSP-3 区间模型：
   `Location { file, line, col, end_line, end_col }`，1-based、半开区间）。

### 2.3 模块边界（0.4 全新设计：参考 Go/Java/Rust，不照搬 0.3）

> **重要（用户明确要求）**：0.4 的 import 机制**不照搬 0.3**（0.3 的 import 是"点号路径 →
> 相对主文件目录的 .shadow 文件"，无真正的包概念），而是**参考 Go/Java/Rust 等成熟语言的
> 模块/包机制设计**。语法形式保留 `import a.b.c`，但**语义与解析机制全新**，详见 §3.8。

- **模块（Module）**：项目级单元（对标 Go module / Rust crate / Java jar）。每个模块有
  `shadow.sbg` 清单（name/version/abi_version/content_hash）+ `shadow.lock` 锁文件。
- **包（Package）**：目录级单元（对标 Go package / Java package / Rust module）。
  一个目录 = 一个包，包内所有 `.shadow` 文件共享命名空间；包名由 `dsb <name>;` 声明。
- **导入（Import）**：`import a.b.c` 解析为"模块路径 + 包路径"（对标 Go 的
  `import "module/sub/pkg"`），而非 0.3 的"相对主文件的文件路径"。
- **可见性**：`pub` = 跨包可见（对标 Go 大写导出 / Rust `pub`）；模块内默认私有；
  包内（同目录文件间）默认可见（对标 Java package-private）。
- **循环依赖**：包级 DAG，编译期检测（对标 Go 的 import cycle 检测）。
- **SPK 包**：作为依赖分发单元（对标 Go module zip / Rust crate），解析、缓存、版本
  约束、锁定的完整机制见 §3.8.4。

---

## 3. shadow-0.3 功能全清单（复刻核对基准）

> 本清单是原则 1 的逐项核对表。每一项在 0.4 中实现后，在本节对应条目后标注 ✅ 并挂接
> 迁移测试。清单覆盖 0.3 全部可观察功能。

### 3.1 语言核心特性（编译器实现的语言）

#### 3.1.1 类型系统（25 种 Type.kind，0.4 全部支持）

| 类别 | 类型 | 说明 |
|------|------|------|
| 标量 | void / bool / char / short / int / long / float / double | int=i32、long=i64、float/double=double 路径 |
| 引用 | string / array\<T\>（含 `T[]` 语法）/ dict\<K,V\>（别名 map/hashmap）/ struct / enum / any / function / tuple / set\<T\> | |
| 0.3 新增 | date / timestamp / nanotimestamp（时间类型）/ opaque（64 位句柄）/ enum_payload（携带负载枚举）/ future（协程句柄）/ infer / unit | 0.4 全部支持 |

#### 3.1.2 字面量

- int（十进制逐位解析）、long（`123L/l` 后缀）、float（`.5`、`1e3`、`1.0e-4` 科学计数）、
  char（`'a'` + 转义映射 ASCII）、string（`\n \t \r \\ \" \0` 转义）、true/false/null。
- 数组 `[]`、结构体 `Name{f:v}`（保留字段名乱序 + 省略字段补默认零值）、元组 `(a,b)`、
  range `a..b`（半开区间）、时间字面量 `date"YYYY.MM.DD"`（数字位拼接为整数）、
  **字符串插值 `"x=${expr}y"`**（parser 级去糖为拼接链）。

#### 3.1.3 运算符

- 算术 `+ - * / %`；比较 `== != < > <= >=`；逻辑 `&& ||`（**短路求值**，经临时 slot）；
- 一元 `- !`；赋值 `=`；复合赋值 `+= -= *= /=`（语法糖展开）；
- `as` 转型；`?` 错误传播（去糖为异常标志检查 + return 默认值）；三元 `cond?then:else`；
- `::` 作用域解析；`..` range；`...` 可变参数标记；
- `.field` / `[idx]` / 方法调用（去糖为 `method(obj,args)`）；元组数字字段 `t.0`；
- `spawn f(args)`（协程创建）、`await expr`（协程等待）。
- 注：0.3 无位运算、无自增/自减。0.5 语法完全复刻 0.3，保持同样能力（如需新增，
  按 §4.1 引导链的演进顺序以"新增特性"处理）。

#### 3.1.4 语句

- `let`（含 `let mut`、元组解构 `let (a,b)=e`、无初始化器结构体自动 `new Type`）；
- if/else（含 else-if）；while；for（`for (vars in iter)`，1/2/多变量，索引 + 元素）；
- match（字面量/通配符走 if 链去糖；**enum 解构走原生 STMT_MATCH**，单/双/多负载 + 臂内 return）；
- return、break、continue；panic（去糖为 `rt_panic_msg`）；
- throw（try 内置标志）/ try-catch（去糖为 `rt_exception_clear/occurred` if 守卫链）；
- **with**（资源管理：块末自动调 Drop 的 `drop()`，重写 return）；
- **defer**（LIFO，重写 return/throw）；裸块语句；表达式语句；赋值语句。

#### 3.1.5 声明

- 泛型函数（`<T: 约束1|约束2>` 联合约束、默认参数、`...args:T` 可变参数）；
- struct / enum（均可泛型；enum 变体携带多负载 `Some(T)` / `Ok(T,E)`）；
- type alias；trait/interface（方法签名 + impl 完整性检查 + 签名兼容）；
- impl（inherent + `impl Trait for Type`，方法名 mangling 唯一化）；
- 全局 `let` / `mut`（`pub let` 跨模块导出）；
- extern 函数（`@extern("c_name")`）；
- **macro**（parse 期内联展开 + hygiene 重命名）；
- 模块声明与 import（`import a.b.c`，点号路径 → 文件路径）；
- **async 协程**（调用自动 spawn 返回 Future，`await` 等待）。

#### 3.1.6 泛型 / trait / 模块

- **真单态化（monomorphization）**：函数级 + 结构体级 + 枚举级，模板深拷贝 + 类型替换 +
  缓存去重；泛型 enum（`Option<T>` / `Result<T,E>`）含函数参数传递与 match 解构；
- ModuleGraph 独立符号表、私有符号 mangling、pub 全局可见、import 递归编译 + 循环检测；
- **.sig 签名缓存**（extract / install / 序列化，`_helper.shadow.sig` 机制）；
- **.lu MIR 缓存**（`mir_serialize`：LEB128 + raw 8 字节，保留全局变量初始值、pub/ext 标志；
  `--mir-cache` 往返验证）。

#### 3.1.7 字符串（0.4 重点重构，见 §7）

- `+` 拼接、`==/!=` 比较、`s[i]` 下标（返回 string）、`len(s)`（按类型分派）；
- 内置：`str_eq/str_contains/str_index_of/str_replace/str_trim/str_to_upper/str_to_lower/
  str_char_at/str_split/str_join/str_format`；
- 数值转换：`int_to_str/long_to_str/float_to_str/bool_to_str/parse_int/parse_long/
  parse_float/parse_bool`；
- 路径/目录：`path_join/path_dirname/path_basename/path_normalize/path_exists/
  dir_create/dir_delete/file_list_dir`（0.3 已具备，0.4 保留）。

### 3.2 编译管线功能

- **Lexer**：完整词法分析（关键字、标识符、数字逐位解析、字符串转义、注释、位置区间）；
- **Parser**：Pratt 表达式解析、语句与声明解析、错误恢复（同步恢复、多错误累积）；
- **TypeChecker**：类型推断与检查、泛型约束求解、单态化实例化、可见性检查、
  结构化诊断（错误码 + file:line:col span）；
- **HIR**：名称→slot 索引、类型查询、复合赋值拆解、块扁平化、`&&/||` 与 if/match 表达式
  临时 slot、闭包捕获变量转 children、range/spawn/await 透传；
- **MIR**：线性指令 + 显式基本块（29+ 种 MIR_OP：ENUM_CONSTRUCT/GET_TAG/GET_PAYLOAD、
  MAKE_CLOSURE/CALL_INDIRECT、SPAWN/AWAIT 等）、loop break/continue 栈、for 去糖为
  while + 隐藏索引变量、`.lu` 二进制序列化/反序列化；
- **Codegen**：全部经 LLVM-C API（`shadow_llvm_*` 桥接层、线程局部 buffer 解决数组参数）、
  `llvm_print_mod` 生成 .ll、main 追加 (argc, argv)、字符串全局 dedup、函数去重
  "最后一次胜出"、GC root 调用点内嵌、GC 安全点；
- **链接**：`llc -O0 -filetype=obj` → 自研运行时 .o + 用户 .o → 链接为 exe（仅系统库，
  见原则 3）；`--run` 直接执行并捕获 stdout。

### 3.3 运行时库功能（rt/ 系统调用层 + src/runtime/ Shadow 层）

- 系统调用层（C，仅系统 API）：内存申请/释放（VirtualAlloc/HeapAlloc 或 mmap 封装）、
  控制台 I/O（stdout/stderr/stdin）、文件 I/O、目录遍历、进程启动/等待、时间、
  线程创建/等待、互斥锁、环境变量、程序参数；
- Shadow 运行时层（编译为 .o 内嵌到产物）：string concat/len/eq、array new/len/cap/grow/
  push*/get*/set*、**any** box/unbox（tag 0=int 1=long 2=float 3=string 4=bool 5=ptr 6=null）、
  to_string 分派、dict/set 操作、时间转换（`*_now` / `*_to_str`）、异常标志
  （`rt_exception_clear/occurred`）、panic（`rt_panic_msg` 打印并退出）、
  `sys_args`（读取命令行参数）；
- 运行时 ABI（沿用 0.3 并修正债务）：string = 长度前缀 + 缓冲（见 §7）；array 布局
  `[len@0][cap@4][elem_size@8][data@12+]`；any = `[tag@0][value@4]`；struct = 运行时分配句柄。
  若 0.5 修改布局必须提升 ABI version 并提供测试迁移计划。
  ⚠️ 目前**没有**自动化 ABI 一致性校验工具，该约束靠人工与链接期报错保障（见 §6.3 待办）。

### 3.4 工具链 CLI（可执行文件 `shadow`）

以下为 `src/main.shadow` 实测支持的全部子命令与参数（2026-08-09 逐项核对）：

| 子命令/参数 | 功能 |
|---|---|
| `shadow <file>` | 编译为 .ll（默认输出 `<file>.ll`） |
| `shadow <file> -o out.ll` | 指定 .ll 输出路径 |
| `shadow <file> --run` | 编译 + llc/clang 链接为 exe + 运行，捕获 stdout |
| `shadow <file> --strict` | **no-op**：严格态已是唯一路径，本 flag 仅为兼容既有 `//@args: --strict` 用例与脚本而保留；可出现在任意参数位置 |
| `shadow --check <file>` | 库模式：仅 lex/parse/tc，输出结构化诊断（stdout 断言） |
| `shadow --diag-json <file>` | 输出 LSP publishDiagnostics JSON（1-based→0-based 区间换算） |
| `shadow --ranges <file>` | 源码区间 dump |
| `shadow --at <file> <line> <col>` | 位置 → AST 节点 |
| `shadow --type-at <file> <line> <col>` | 位置 → 类型 |
| `shadow --lsp` | 长驻 LSP 服务器（JSON-RPC over stdio，117 项功能，见 §3.6） |
| `shadow pack <dir> <out.spk>` | 目录打包为 .spk（ZIP STORE 格式） |
| `shadow unpack <pkg.spk> <dir>` | .spk 解包 |
| `shadow list <pkg.spk>` | 列出 .spk 包内文件 |
| `shadow -init <name>` | 脚手架：生成 shadow.sbg + main.shadow 工程模板 |
| `shadow --version` | 打印 `shadow 0.5` |

**查询类 CLI 的严格态语义**：`--check` / `--diag-json` / `--ranges` / `--at` / `--type-at`
全部走 `main_load_combined`——即 import 递归加载 + `dsb` 校验 + 严格态类型检查，
与正常编译路径完全一致。主文件被放在 combined 缓冲最前（保证行号不偏移），
诊断计数与 range 只统计主文件模块（`api_set_main_end`，边界取 `tc_mod_starts[1]-1`）。

**已废弃、不再存在的参数**（0.3 时代的运行时缓存三件套）：
`--no-runtime-cache` / `--runtime-cache <dir>` / `--emit-runtime-cache <dir>`。
运行时不再是独立预编译单元——`src/runtime/runtime_lib.shadow` 作为普通模块直接参与
combined 编译。2026-08-05 核查确认 `build/runtime.o` 从未出现在任何链接行，
相关步骤已从 `build_shadow.sh` 与 `tools/build.sh` 移除。

### 3.5 SBG 构建系统 / semver / SPK

- **SBG**：TOML 微子集，`[package]`（name/version/abi_version/content_hash）+
  `[deps]`（`path@constraint`）+ `[native]`（sources/links/link_dirs）；
  `shadow.lock` lockfile（`[[dep]]` 锁定 name/path/version/content_hash）；
  传递依赖递归加载 + 循环防护；dep 版本约束校验。
- **semver**：semver 2.0.0 完整实现（`MAJOR.MINOR.PATCH[-pre][+build]`）；
  约束 `* / >= / < / >=a,<b / ^caret / = / 裸版本`；prerelease 逐标识符比较（数字<字母、
  字段少者低）；build 不参与比较。
- **SPK**：zip 容器（内置 STORE 压缩，不依赖外部 miniz——miniz 源码静态并入或自研实现，
  但**不得作为产物程序依赖**，见原则 3）；解包缓存 `_spk_extracted`；编译时自动展开并
  收集 `[native]` 源文件编译为 .o 追加到链接命令；ABI sentinel 校验。

### 3.6 LSP 服务器（117 项功能，全部复刻）

> 0.3 的 `src/lsp/lsp.shadow`（6.5k 行）实现 55+ LSP 方法、117 项功能。0.4 必须全量复刻，
> 功能类别如下（逐项核对表以 0.3 的 `docs/FEATURES.md` 为准）：

| 类别 | 功能数 | 关键功能 |
|------|--------|----------|
| 语法与编辑体验 | 6 | TextMate 语法高亮、括号匹配、自动缩进、折叠、注释、选区扩展 |
| 代码补全 | 13 | 函数/结构体/枚举/关键字/类型/成员/结构体字面量/局部变量/跨文件补全、snippet、前缀过滤、sortText、resolve、触发字符 |
| 悬停与签名 | 9 | Markdown 悬停、函数/结构体/枚举/变量悬停、文档注释提取、签名帮助、活跃参数追踪 |
| 跳转导航 | 7 | 定义/声明/类型定义/实现跳转、文档高亮、文档链接（四级回退路径解析） |
| 引用与重命名 | 3 | 跨文件引用查找、跨文件重命名、prepareRename（关键字拦截） |
| 代码操作与透镜 | 11 | 快速修复（缺 return/未定义变量/缺括号/类型转换）、codeAction/resolve、代码透镜、executeCommand、applyEdit |
| 格式化 | 5 | 全文/范围/输入时/保存时格式化 + 配置（tabSize/insertSpaces） |
| 诊断 | 7 | push + pull（LSP 3.17）、工作区诊断、import 链诊断、版本追踪、refresh、配置（warnOnly/maxItems） |
| 语义着色 | 4 | full / delta（增量）/ range + refresh（9 种 token 类型） |
| 内联功能 | 5 | inlayHint（变量类型内联）、inlineCompletion（函数调用骨架）、inlineValue、resolve、refresh |
| 类型与调用层次 | 6 | prepareTypeHierarchy/supertypes/subtypes、prepareCallHierarchy/incomingCalls/outgoingCalls |
| 符号与导航 | 7 | documentSymbol、workspace/symbol（深度 3 递归）、workspaceSymbol/resolve、foldingRange、selectionRange、documentHighlight |
| 颜色与联动 | 4 | documentColor（#RGB/#RRGGBB/#RRGGBBAA）、colorPresentation、linkedEditingRange、moniker |
| 文件操作 | 10 | willCreate/didCreate/willRename/didRename/willDelete/didDelete、原生文件监听（ReadDirectoryChangesW 或等效）、mtime 轮询、四级 import 路径解析、didChangeWatchedFiles |
| 工作区与进度 | 14 | workspaceFolders 多根、配置拉取/推送、$/progress 工作流、取消、showMessage/Request、logMessage、showDocument、telemetry、setTrace/logTrace、cancelRequest、4 种 refresh |
| SPK 包管理 | 6 | pack/unpack/list、SPK import 解析（幂等解包缓存）、SPK 依赖、SPK native 文件收集 |

### 3.7 债务表（0.3 债务 0.4 已解决；0.5 债务清单）

**0.3 已知债务（0.4 已解决，2026-08-08 全绿验证）**：

| 债务 | 0.3 现状 | 0.4 处理 |
|------|----------|----------|
| 产物依赖 LLVM-C.dll | 0.3 目标程序经 runtime_for_selfhost.o 引 shadow_llvm_* 符号，加载期需解析 LLVM-C.dll | **修正**（原则 3）：LLVM-C.dll 是编译器生成 .ll 的**必要工具链依赖（保留）**；产物程序不链接 |
| 字符串拼接内存膨胀 | NUL 结尾 C 串 + 裸 concat | **§7 方案**（原则 9） |
| GC 与 Go 差距 | STW 分代 + 保守扫描 + 手工 root | **§5 方案**（原则 7） |
| any 盒双布局 | Shadow/C++ 两套历史表示 | 收敛为单一规范 |
| 运行时双来源 | runtime_lib.shadow + shadow_runtime_minimal.cpp | 统一为 rt/ 系统调用层 + Shadow 层单一来源 |
| 无位运算/自增自减 | 0.3 缺失 | 语法完全复刻 0.3，保持缺失（如需新增按 §4.1 引导链演进顺序） |

**0.5 新债务（本迭代必须解决）**：

| 债务 | 0.4 现状 | 0.5 处理 |
|------|----------|----------|
| 兼容态/严格态双轨 | `tc_check_program`（物理拼接）与 `tc_check_program_strict` 并存，`ctx.strict` 全代码分叉 | ✅ **已解决（2026-08-09）**：见下方「债务① 收口记录」 |
| `--strict` flag | 需显式传入才启用严格态 | ✅ **已解决（2026-08-09）**：严格态即默认；`--strict` 保留为 no-op（兼容 `//@args: --strict` 用例与既有脚本） |
| 跨模块泛型缓存判定 | `main_has_generic` 只扫本模块源码 `<X`，A 模块调用 B 的泛型函数时 A 仍可命中缓存 → 特化缺失 | ✅ **已解决（2026-08-09）**：改为 AST 全程序判定 `main_prog_has_generic`；连带**复活 `.lu` 增量缓存**（自编译 322s → 117s）。见下方「债务③ 收口记录」 |
| LSP hover 回显 mangle 名 | `api_fmeta` hover 签名未过 `api_demangle`；`api_func_sig` 死代码 | 统一 demangle；删死代码。<br>现状核查：`api_fmeta` 构造处（compile_api.shadow:442）**已过 `api_demangle`**；剩余项 = 删除死代码 `api_func_sig` |

#### 债务① 收口记录（2026-08-09）

删除范围（净 −118 行，4 文件）：

| 位置 | 删除内容 |
|------|----------|
| `type_checker.shadow` | `ctx.strict` 全部 **28 处**分叉（保留严格态分支体，不折叠成常量——严格态 `if` 要求 bool，折 `1` 会报 `condition must be bool`） |
| `type_checker.shadow` | `tc_check_program_mode(prog, strict)` 的 mode 形参；函数更名为唯一入口 `tc_check_program(prog)`；`tc_check_program_strict` 降为薄别名 |
| `type_checker.shadow` | `tc_node_lookup_func` / `tc_node_lookup_global` 的 `while (ci < 2)` 兼容态回退轮（严格态首轮即 `break`，等价改写为单轮精确匹配） |
| `type_checker.shadow` | 全局 `tc_g_strict`；`tc_set_strict` 退化 no-op、`tc_is_strict()` 恒 `1` |
| `core/ast.shadow` | `CompileContext.strict: int` 字段 |
| `main.shadow` | 全局 `main_g_strict` 及其 6 处分支（循环 import、dsb 别名映射、dsb 强制、TC 入口、2 处缓存门控） |
| `api/compile_api.shadow` | `tc_set_strict(0)` 初始化 + `tc_is_strict()` 双分支派发 |

关键设计决策：`.lu` 缓存的两处门控（`main_lu_prepare` / `main_lu_store`）**不是语义双轨而是性能开关**，
若直接展开成无条件 `return` 会让整段缓存实现变成不可达死代码。改为独立门控
`let main_g_lu_cache: int = 0;`，保留代码路径。
→ **后续**：该门控已于 2026-08-09 置 1 复活，见「债务③ 收口记录」。当时判断的复活前置条件
（"待缓存支持符号 mangle"）事后证明是**误判**——mangle 发生在 Pass 1 声明注册，与缓存命中与否无关，
真正的阻塞点是闭包编号跨模块漂移与泛型判定过窄。

验证（唯一可信判据 = 自举对照实验）：

- stage2（f87130b 的 `build/shadow.exe` 编译改后源码）→ `d1_stage2.ll` = 10,980,114 B，RC=0
- stage3（stage2 链出的 `d1.exe` 再编译同一份源码）→ `d1_stage3.ll` = 10,980,114 B，`cmp` **逐字节相同**
- 全量回归 `SHADOW_EXE=build/d1.exe python test/run_tests.py -j 8` → **PASS=923 FAIL=0 CRASH=0 SKIP=2**（与 f87130b 基线逐项一致）
- 对照基线：f87130b `stage2.ll == stage3.ll` = 11,044,021 B，`shadow.exe` = 1,756,672 B；改后 `d1.exe` = 1,748,992 B

#### 债务③ 收口记录（2026-08-09）— AST 级泛型判定 + `.lu` 增量缓存复活

**背景**：债务表原把"跨模块泛型缓存判定"记为「因缓存已关，暂无运行期影响」。真正动手才发现
它与"缓存为何不敢开"是同一个问题的两面——缓存关着，是因为判定不可靠；判定不修，缓存就不能开。

**改动**（`main.shadow` +128/−38，`type_checker.shadow` +58）：

| 位置 | 内容 |
|------|------|
| `main.shadow:1016` | 新增 `main_prog_has_generic(prog: Program) -> int`：遍历 `funcs/structs/enums/traits(含 methods)/impls(含 methods)` 的 `len(type_params) > 0`，任一命中即返回 1 |
| `main.shadow:1064` | 新增 `main_cache_revoke()`：清零 `main_g_valid` + `tc_set_valid_ns([])` |
| `main.shadow:1355` | `parse` 之后立即 `if (main_prog_has_generic(prog) == 1) { main_cache_revoke(); }` |
| `main.shadow` `main_cache_setup` | 删除 Pass 2 的文本扫描闸门（旧写法只认 `<T:`/`<T>`/`<T `，漏 `<U>`/`<K, V>`，更看不见使用方的实例化点），改为**乐观命中 + 事后撤销** |
| `main.shadow:1217` | 缓存键世代 `v9_s6_` → `v10_s1_`，与 0.4 兼容态旧缓存物理隔离 |
| `main.shadow:96` | `main_g_lu_cache` **0 → 1** |
| `type_checker.shadow:1118` | `tc_ns_is_valid` 删除首行硬编码 `return 0;`（见下方「两道闸」） |
| `type_checker.shadow:4393` | 闭包命名改为按模块计数 `__closure_<mod>_<n>`（新增 `tc_closure_seq_next`/`tc_closure_mod_tag`），消除跨模块编号漂移 |

**两阶段式撤销的必要性**：`main_cache_setup` 必须跑在 `parse` 之前（它要为命中模块准备 MIR 队列），
而 AST 判定只能在 `parse` 之后。故只能先乐观命中、拿到 AST 再整体撤销。撤销是全程序级
（**任一**泛型定义即禁用**全部**模块缓存）——保守但绝对安全；编译器自身零泛型定义，
自举场景不受影响。

**🔴 关键教训：缓存有两道闸，只开一道会得到最具迷惑性的假象**

1. `main.shadow` 的 `main_g_lu_cache`（总开关）
2. `type_checker.shadow` 的 `tc_ns_is_valid` 首行 `return 0;`（把查表逻辑变成不可达死代码）

只开第 1 道时：`main_g_valid` 正常置位、`.lu` 确实命中（文件 mtime 不再刷新）、MIR 队列正常复用——
**唯独占编译耗时 63.7% 的 TC 函数体检查从未跳过**。表现为"缓存命中却几乎不提速"
（实测冷 318s vs 热 310s，仅省 2.5%）。定位手段是加 `TIMING-DBG-TEMP` 阶段埋点
（存在 `build/.timing` 文件时打印 `tok/parse/tc/hir+mir/cg/total`），一眼看出 `tc` 冷热几乎不变。

**跳过函数体检查的正确性依据**：命中条件是「本模块源码 hash 未变 **AND** 世界签名 WSIG 未变」，
即本模块代码与所有模块的签名都没变；符号 mangle 发生在 Pass 1 声明注册（`tc_reg_name_for` +
`fd.name` 回写），与命中与否无关、照常执行；被跳过模块的 MIR 整体来自 `.lu`，其 AST 后处理
（spawn 的 `e.str1` 回写、内置遮蔽改写、`inferred_type` 填充）都无下游消费者；闭包提升缺失由
`main.shadow` 的闭包 MIR 补取兜底；泛型单态化缺失由 `main_prog_has_generic` 整体撤销排除。

**设计约束（勿破坏）**：`main_take_cached_mir` 按**位置**弹出 `q[pos]`，无名字校验，
依赖「存/取两次模块内函数枚举顺序一致」。当前成立是因为 `extern`（`len(fd.extern_name)>0`）
与泛型模板都在 `use_cache` 判定**之前** `continue`，两侧对称跳过；TC 追加的函数（特化/闭包）
由 `fi < N0`（`N0 = n_funcs_pre_tc`）排除在缓存路径外。若将来改动 `prog.funcs` 的枚举顺序或
extern 判定，必须同步给缓存条目加名字校验。

**验证（自举对照实验 + 缓存透明性）**：

- stage2 = `d4_stage2.ll` **11,010,197 B**（由 d3.exe 空缓存编译本轮源码所得）
- stage3 = `d4.exe` 冷缓存编译同一源码 → **11,010,197 B**，`cmp` 逐字节相同 → 跨两个不同引导器
  （d3 → d4）收敛到同一不动点，比同引导器自比更强
- stage4 = `d4.exe` 热缓存（14 个 `.lu` 全命中）→ **11,010,197 B**，与 stage3 逐字节相同
  → **缓存对产物完全透明**
- stage5 = 再热一次 → 与 stage4 相同，热运行可重复

**提速实测**（同一 `d4.exe`，单位 ms）：

| 阶段 | 冷 (stage3) | 热 (stage4) | 倍数 |
|------|------------|------------|------|
| tok | 12,841 | 14,241 | ~1.0 |
| parse | 64,245 | 65,237 | ~1.0 |
| **tc** | **200,905** | **3,036** | **66×** |
| hir+mir | 2,841 | 27 | 105× |
| cg | 34,390 | 34,477 | ~1.0 |
| **total** | **315,222** | **117,018** | **2.69×** |

剩余耗时结构：parse 55.7%、cg 29.5%、tok 12.2% —— 下一个优化目标转为 parser 与 codegen。

---

### 3.8 import / 模块 / SPK 包机制（0.5 唯一路径，参考 Go/Java/Rust）

> **用户明确要求**：import 机制**不要照搬 0.3**，参考 Java/Go/Rust 等成熟语言的
> 机制；**特别注意 SPK 包的处理**。0.4 曾以兼容态/严格态双轨过渡（兼容态保留 0.3 物理拼接），
> **0.5 删除兼容态，本节的严格态机制是唯一路径**。本节是 0.5 模块系统的完整设计规范。

#### 3.8.1 设计目标（对标成熟语言）

| 语言 | 借鉴点 |
|------|--------|
| **Go** | 模块（module）+ 包（package）两级模型；`go.mod` 声明依赖；module cache 目录；import path 与目录结构强绑定；import cycle 检测 |
| **Rust** | crate = 编译单元；`Cargo.toml` + Cargo.lock 锁依赖版本；registry 缓存；`pub`/`pub(crate)` 可见性分层 |
| **Java** | package 命名空间隔离（package-private）；jar 包分发；Maven 坐标（groupId:artifactId:version） |
| **0.3 对比** | 0.3 的 import 是"点号路径 → 相对主文件目录的 .shadow 文件"，**无模块/包概念**，SPK 仅是 zip 容器 |

#### 3.8.2 模型

```
Module（模块，对标 Go module / Rust crate / Maven 坐标）
  ├── shadow.sbg（清单：name / version / abi_version / content_hash / [deps] / [native]）
  ├── shadow.lock（锁文件：锁定依赖的 name/path/version/content_hash）
  └── Packages（包，对标 Go package / Java package）
        └── 目录 = 包；包内所有 .shadow 文件共享命名空间（package-private 默认可见）
```

- **路径模型（对标 Java package，2026-08-06 修订）**：`import a.b.c` 的**点号即目录分隔符**，
  从**源码根（主文件所在目录）**按嵌套目录解析为 `a/b/c.shadow`。**没有"首段=模块名"概念**
  （此前 §3.8.2/3.8.3 写的"首段 a 是模块名、对标 Go module path 首段"是错误模型，已推翻）。
  `a` 自然成为顶层目录，与 Java `package a.b` = 目录 `a/b/` 完全一致。
  - **解析优先级**：① 源码根本地路径 `a/b/c.shadow`；② 未命中则查 `shadow.sbg [deps]`（首段
    作为外部依赖名，对标 Go `require` / Cargo `[dependencies]` / Maven groupId，走 SPK 或目录）；
    ③ SPK 包缓存（见 §3.8.4）。SPK 机制完整保留，仅不再靠"首段识别模块"。
- **包 = 目录**：`a/b/` 即包 `a.b`；编译单元 = 文件 `a/b/c.shadow` 即 `a.b.c`。
  `import a.b.c;` 引入该文件全部 pub 符号；`import a.b.*;` 引入 `a/b/` **直接**子文件的 pub
  符号（**不递归**子目录，Java 规则）。
- **符号可见性**：`pub` = 跨包导出；**未修饰 = 同目录（同包）可见**（package-private，Java 规则）；
  跨包不可见。符号注册按完整模块路径 mangle（文件级唯一），故同目录多文件各定义同名私有符号
  不会 redefinition。不传递：A→B→C 时 A 不能用 C 的简名（可用 C 的 FQN `a.b.c.d()`）。
- **dsb 强制（Java 规则，0.5 始终强制）**：`dsb <name>;` 的 `<name>` 必须 == 文件 basename
  （不含 `.shadow`），对应 Java「class 名 == 文件名」；包（命名空间）由目录路径自动派生。
  **0.5 无兼容态豁免**（0.4 曾仅 `--strict` 强制，兼容态不检查；0.5 已删除兼容态）。

#### 3.8.3 import 解析算法（对标 Java package resolution：点号=目录）

```
resolve_import(import_path, project_root):
  1. 整条路径按 '.' 拆为嵌套目录：a.b.c → a/b/c.shadow（从 project_root 起算）；
     点号即目录分隔符，无"首段=模块名"特殊处理。
  2. 本地命中 <project_root>/a/b/c.shadow（或 a/b/ 目录）→ 收集
  3. 否则查 shadow.sbg [deps]（首段作外部依赖名）:
       a. dep.path 以 .spk 结尾 → 走 SPK 包处理（§3.8.4）
       b. dep.path 为目录 → <dep_dir>/<rest>.shadow
       c. 无 manifest 依赖 → 查全局模块缓存
  4. 解析失败 → 报错并给出候选依赖提示（对标 Java "package not found"）
  5. 循环检测：见下（严格态放开，兼容态保留）
```

**循环检测规则（0.5 唯一：对标 Java，允许循环 import）**：
- **Java 规则**：`a.Foo` 引用 `b.Bar` 同时 `b.Bar` 引用 `a.Foo` 是**合法**的——javac 一次性
  读入全部源文件、在 Enter 阶段统一建符号表，非逐文件顺序编译。JDK 自身大量互引。
  Java **真正禁止的是循环*继承***（`class A extends B` + `class B extends A`），那是另一回事。
- **0.5 实现**：放开循环 import（`main_collect_module` 遇已在 `path_stack` 的模块
  直接 return 1，靠 `compiled_set` 去重防重复编译）。**0.4 兼容态的 `circular import detected`
  已随兼容态删除**（0.5 无此分支）。
- 菱形依赖（a→c、b→c，c 被两条路径访问）始终合法：`compiled_set` 命中 → 跳过重编译，
  但不在同一条 `path_stack` 路径上 → 不构成环。

#### 3.8.4 SPK 包处理（重点）

SPK = **依赖分发单元**（对标 Go module zip / Cargo crate / Maven jar）。0.4 完整机制：

| 环节 | 机制 | 对标 |
|------|------|------|
| 打包 | `shadow pack <dir> <out.spk>`：目录 → ZIP STORE 容器（含 shadow.sbg 清单 + 源码 + [native]） | Cargo package / Maven jar |
| 安装缓存 | 首次使用自动解包到**全局缓存目录**（对标 GOMODCACHE：`$SHADOW_HOME/pkg/mod/<module>@<version>/`），**幂等**（已存在则复用） | Go module cache |
| 版本解析 | 缓存键 = `module@version`；版本约束来自 shadow.sbg [deps]（semver，含 caret/prerelease） | Cargo registry / Maven version ranges |
| 校验 | 解包后校验 content_hash + abi_version（防篡改/防 ABI 漂移） | Go `go.sum` / Cargo checksum |
| 锁定 | shadow.lock 记录实际解析版本（对标 Cargo.lock / go.sum） | Cargo.lock |
| 传递依赖 | 递归加载 SPK 内 shadow.sbg 的 [deps]，循环防护 | Cargo 传递依赖 |
| native 支持 | SPK 内 [native] 节收集 C/C++ 源，编译为 .o 追加链接 | — |
| import 解析 | `import a.b.c` 且 a 是 SPK 依赖 → 从解包缓存目录读 `c.shadow` | Go module 内 import |
| 过期策略 | 版本不变则缓存永不过期；换版本 → 新缓存目录（版本化，不覆盖） | Go module cache（版本隔离） |

**与 0.3 的差异**：
- 0.3：SPK 解包到**项目内** `_spk_extracted` 临时目录，无版本化缓存，无锁文件语义
- 0.4：SPK 解包到**全局版本化缓存**（`$SHADOW_HOME/pkg/mod/<module>@<version>/`），
  shadow.lock 锁定，semver 约束解析，content_hash + abi_version 双重校验

**SPK 内部布局**（pack 时固化，unpack 时还原）：
```
out.spk
├── shadow.sbg          # [package] name/version/abi_version/content_hash + [deps] + [native]
├── src/                # 包源码（.shadow）
├── native/             # [native] 节引用的 C/C++ 源
└── .spk_meta/          # 打包元数据（可选）
```

#### 3.8.5 0.5 编译器源码自身的组织（严格态 import 为唯一路径）

> 0.5 编译器源码（`src/`）**自身按严格态规则编写**（模块目录 + import 点号路径 + `pub` 可见性），
> 由 0.5 自举链（`build/shadow.exe` 冷启动宿主，见 §4.1）逐级编译。
> 严格态语义（可见性 / mangle）由 0.5 编译器自身的 TC 实现。**0.5 无兼容态分支，TC 层
> 只有严格态路径**（0.4 曾按 `ctx.strict` 分叉，0.5 已删除 `ctx.strict==0` 全部分支）。

#### 3.8.6 import 机制变化与自举的关系（分析结论）

**结论：import 机制变化不影响自举可行性，但有两个前提 + 一处风险。**

两层 import 严格区分：
- **L1：0.5 编译器源码自身的 import**（`src/*.shadow` 里的 `import core.ast`）。
  由 `build/shadow.exe`（上一轮 fixed-point 产物）编译 → 按严格态解析；stage2/stage3 由 0.5 编译器自身逐级编译 → 同样按严格态解析。
- **L2：0.5 语言给用户程序的 import**（0.5 编译器实现的特性）。与 L1 同一套严格态机制。

fixed-point（`stage2.ll == stage3.ll`）只要求 **stage2 与 stage3 行为一致**；
二者由同一份源码、同一套严格态机制编译，天然一致。stage1 与 stage2 的 .ll
**本来就不要求相等**（不同宿主编译），fixed-point 从不比较 stage1。

**前提 A（自举宿主同源）**：0.5 源码的 import 写法（`import a.b.c`）须能被 0.5 自举链解析——
源码组织保持"模块目录 + 点号路径"，严格态机制只换解析语义、不改语法形式（§3.8 已如此设计）。

**前提 B（自我兼容）**：0.5 严格态机制必须能解析自身源码的 import——`import core.const`
直接按点号嵌套目录解析为源码根 `core/const.shadow`（无需"首段识别为主模块"特殊分支），
否则 stage2/stage3 编译源码失败，自举断裂。（已在 2026-08-09 fixed-point 达成中验证通过。）

**风险（外部状态）**：严格态机制若依赖全局模块缓存 / shadow.lock / SPK 解包结果等易变外部
状态，stage2 与 stage3 运行时状态不同 → 编译结果不同 → fixed-point 失败。
对策：
- 自举链上 0.5 源码**只用本地 import**（主模块 + 本地目录），不碰 SPK/外部依赖；
- 严格态机制的"主模块自身 + 本地包"解析路径必须**纯确定性**（输入源码路径 → 输出文件内容）；
- SPK/模块缓存查找只影响用户程序，不进 fixed-point 比较路径；
- 自举通过后，0.5 源码再逐步使用 SPK 等新特性（先实现 → stage1 支持 → stage2 使用），
  每次演进后重放 stage1→stage3 并比对。

**演进路线**：
```
stage1（build/shadow.exe 冷启动宿主编译） → 0.5 严格态机制解析源码 ✓
stage2（shadow-stage1 编译）             → 0.5 严格态机制解析同一份源码，须通过前提 B ✓
fixed-point                              → stage2 == stage3（同机制，天然成立）
演进                                    → 严格态机制实现后，0.5 源码逐步使用新特性，每次重放 fixed-point
```

---

## 4. 自举方案

### 4.1 引导链

0.5 的引导链**不涉及任何历史宿主**：冷启动宿主就是仓库内已有的 `build/shadow.exe`
（上一轮自举产物）。整条链只在 0.5 自身源码上循环，直到达成不动点。

```
build/shadow.exe（冷启动宿主 = 上一轮自举产物，已在仓库内）
   │  编译 src/main.shadow（严格态 import 递归加载全部模块）
   ▼
build/stage1.ll → build/shadow-stage1.exe
   │  编译 src/main.shadow（同一份源码）
   ▼
build/stage2.ll → build/shadow-stage2.exe
   │  编译 src/main.shadow（同一份源码）
   ▼
build/stage3.ll
   │
   └── 验收：stage2.ll == stage3.ll（逐字节相等，fixed-point）
       通过后 build/shadow-stage2.exe 复制为 build/shadow.exe，成为下一轮的冷启动宿主
```

- **唯一构建入口**：仓库根的 `build_shadow.sh`（7 步，见 §4.3）。耗时取决于 `.lu`
  缓存冷热：热缓存下每阶段约 2 分钟、全链约 6 分钟；冷缓存下每阶段约 5 分钟
  （见 §3.7 债务③ 的实测表：冷 315s / 热 117s）。
- **验收标准**：`tools/verify_fixed_point.py build/stage2.ll build/stage3.ll` 逐字节相等。
  实践中 stage1 通常也与 stage2/stage3 相等——因为冷启动宿主本身已是不动点产物。
- **自我承载性**：一旦 fixed-point 达成，`build/shadow.exe` 被自身产物覆盖，
  下一轮构建的宿主即是本轮产物；引导链不再依赖仓库外的任何编译器。

> **⚠️ 引导链单点脆弱性（已知风险，未解决）**：`.gitignore` 中包含 `build/`，
> 因此冷启动宿主 `build/shadow.exe` **不受版本控制**。一旦被误删或清理，
> 仓库内没有任何可直接编译 0.5 严格态源码的工件，引导链断裂。
> 目前的唯一兜底是从 `shadow-0.4/build/shadow.exe` 重新取一份宿主。
> **建议**：把当前可用的 `shadow.exe` 快照归档到 `bootstrap/`（不受 ignore 影响）
> 并纳入版本控制，作为可重放的冷启动锚点。

### 4.2 bootstrap/ 冻结工件清单

`bootstrap/` 只承担**链接期**职责——提供 0.5 自身尚未用 Shadow 重写的运行时对象。
它不再承担"宿主编译器"职责。

| 工件 | 是否参与当前构建 | 用途 |
|------|-----------------|------|
| `runtime_for_selfhost.o` | ✅ 每个 stage 的链接行 | 宿主运行时（含 `shadow_stdin_read_line` 等 6 个 I/O 符号） |
| `miniz.o` | ✅ 每个 stage 的链接行 | SPK ZIP 支持 |
| `miniz.h` | ✅ 编译 `rt/rt_zip.c` 时 `-I bootstrap` | miniz 头文件 |
| `runtime_clang.o` | ❌ 未出现在任何链接行 | 历史遗留，可清理 |
| `s03.exe` | ❌ 引导链已不使用 | 历史遗留，可清理 |

> **链接行约束（勿破坏）**：`rt_io.o` **不得**加入 `shadow.exe` 的链接行——
> `runtime_for_selfhost.o` 已内置同名符号，重复链接会 duplicate symbol。
> `rt_io.o` 只在 `[6/7]` 编译，供 `tools/link_rt.py` 链接**用户程序**时使用。

### 4.3 `build_shadow.sh` 步骤定义

| 步骤 | 内容 | 产物 |
|------|------|------|
| `[0/7]` | `clang -I bootstrap -c rt/rt_zip.c` | `build/rt/rt_zip.o` |
| `[1/7]` | `build/shadow.exe src/main.shadow -o build/stage1.ll` → llc → clang++ 链接 | `build/shadow-stage1.exe` |
| `[2/7]` | `shadow-stage1.exe` 编译同一份源码 → llc → 链接 | `build/shadow-stage2.exe` |
| `[3/7]` | `shadow-stage2.exe` 编译同一份源码（仅出 IR） | `build/stage3.ll` |
| `[4/7]` | `tools/verify_fixed_point.py stage2.ll stage3.ll` | fixed-point 验收 |
| `[5/7]` | `cp shadow-stage2.exe shadow.exe` | **最终产物 `build/shadow.exe`** |
| `[6/7]` | 编译 `rt/` 系统调用层 10 个 .c | `build/rt/*.o`（供 `link_rt.py` 链用户程序） |
| `[7/7]` | `shadow.exe --version` 冒烟 | — |

**统一链接行**（stage1/stage2 相同）：

```
clang++ -std=c++17 build/stageN.o \
    bootstrap/runtime_for_selfhost.o bootstrap/miniz.o \
    build/rt/shadow_gc_supplement.o build/rt/rt_zip.o \
    -o build/shadow-stageN.exe -Wl,/subsystem:console -lws2_32 -lLLVM-C -L"$LLVM/lib"
```

**已知脚本缺口**：`build/rt/shadow_gc_supplement.o` 出现在链接行，但 `[6/7]` 的编译
循环里**没有** `shadow_gc_supplement`——脚本依赖该 .o 已存在于 `build/rt/`。
干净 checkout 后首次构建会因缺少该对象而链接失败，需先手工编译：
`clang -O0 -I bootstrap -c rt/shadow_gc_supplement.c -o build/rt/shadow_gc_supplement.o`。

### 4.4 自举验证纪律（唯一可信判据）

**"0.5 能编译通过" 不等于 "改动正确"**。编译通过、`--check` 全绿、IR 合法、exe 链接成功，
这些全部满足时改动仍可能在自举时爆出数千条错误（历史教训见 §3.7）。唯一可信判据是
**自举对照实验**：

1. 同一冷启动宿主分别编译**改前**与**改后**源码，各得一个 exe；
2. 让这两个 exe **各自再编译各自的源码**；
3. 比较错误数与产物字节数。

只有改后 exe 能编译自身源码并与上一阶段逐字节收敛，才算通过。

**配套要求**：

- 改动前用 `cp -r src build/_orig_src_$(date +%s)` 留对照组；
- 改 `src/` 后若怀疑缓存干扰，归档式清理 `.lu`
  （`find src -name '*.lu' -exec mv {} build/_old_lu_<ts>/ \;`），不要用 bulk delete；
- 构建耗时长，**必须后台运行**并重定向日志，不要在前台等待。

### 4.5 自举风险与对策

| 风险 | 对策 |
|------|------|
| 冷启动宿主自身携带 miscompile | 每轮都跑满 stage1→stage3；不动点只能证明"稳定"，新宿主接管后立即跑全量回归（`test/run_tests.py`）交叉验证 |
| 改动破坏 fixed-point | fixed-point 校验跑在"同一份源码"上；任何语义改动后重放完整链条，逐字节比对 |
| LLVM 版本漂移 | 固定 `D:\llvm\clang+llvm-22.1.0-x86_64-pc-windows-msvc`；升级需全量重放 stage1→stage3 并比对 |
| `.lu` 缓存掩盖改动 | 缓存键含源码 hash 与世界签名 WSIG，源码变更自动失效；若出现"改了没用"的假象，先归档 `.lu` 重建确认 |
| **`build/shadow.exe` 被误删** | ⚠️ **当前无仓库内兜底**（`build/` 被 gitignore）。临时办法：从 `shadow-0.4/build/shadow.exe` 取宿主。待办：把宿主快照归档进 `bootstrap/` 并纳入版本控制 |
| `bootstrap/` 运行时对象与 Shadow 层 ABI 漂移 | `runtime_for_selfhost.o` 是二进制冻结件，改动 `rt/` 或内建符号签名时须同步确认无符号冲突/缺失，链接期报错即暴露 |

## 5. GC 系统设计（完全对标 Go）

### 5.1 Go GC 架构基线（0.5 对标目标）

| 维度 | Go GC（go1.21+） | 说明 |
|------|------------------|------|
| 算法 | **并发三色标记 + 清扫（concurrent tri-color mark & sweep）** | 标记与用户程序并行 |
| 代际 | 非分代（non-generational） | 不分年轻代/老年代 |
| 压缩 | 非压缩（non-compacting） | 对象地址稳定 |
| 写屏障 | **混合写屏障（hybrid write barrier，Go 1.8+）** | 插入 + 删除屏障结合，消除二次 STW 全栈重扫 |
| 标记 | 并行标记（多个 P 的 mark worker + work stealing） | 分摊标记工作 |
| 触发 | **堆增长比例（GOGC，默认 100%）+ 最小堆阈值** | 分配辅助（GC assist） |
| 清扫 | 惰性清扫（lazy sweep）+ 后台清扫 | 分配时按需清扫 |
| 根集 | **精确**：编译器生成 stack map / register map + 全局变量 map | 无保守扫描 |
| 停顿 | 标记终止双 STW 极短（目标 <1ms 量级） | 全局栈/寄存器快照 |
| 分配器 | 空闲列表 + size class（mheap/mspan/mcache） | 线程本地缓存 |

### 5.2 Shadow 0.5 GC 设计

#### 5.2.1 内存布局与对象模型

- 对象头（8 字节对齐）：`[gc_flags:u8][type_index:u32][size:u32]`（或等效紧凑布局），
  其中 `type_index` 指向全局类型表 → 决定该对象内部哪些偏移是指针（精确追踪，无保守扫描）；
- 堆按 size class 分桶（对标 mspan/mcache），线程本地分配缓存 + 全局空闲列表；
- 从操作系统批量申请（Windows `VirtualAlloc` / Linux `mmap`），GC 归还 OS 使用
  `VirtualFree`/`munmap`（对标 Go `scavenge`）。

#### 5.2.2 三色标记

- 白 = 未发现；灰 = 已发现未扫描；黑 = 已扫描；
- 标记栈：每线程本地灰队列 + 全局共享队列（work stealing 简化：全局队列 + 锁，或
  per-thread 队列 + 原子窃取）；
- 并发标记 worker：GC 开始时唤醒 N 个标记线程（N = 逻辑核数），与 mutator 并行；
- 对象图遍历：按类型表精确扫描指针字段（struct 字段、array 元素、any 盒 value、dict 键值）。

#### 5.2.3 写屏障（编译器插入）

- 采用 **Go 1.8 混合写屏障**语义：
  - 插入屏障：`*slot = obj` 时，若 `slot` 指向老对象且 `obj` 为白 → 立即置灰；
  - 删除屏障：标记期间被覆盖的旧值若为白 → 立即置灰（防止漏标）；
  - 混合策略消除"标记终止全栈重扫"需求（或保留一次极短 STW 精确扫描最终状态）；
- 由编译器在 MIR 的 store 指令前插入写屏障调用（对标 Go 编译器 `writeBarrier` 插入）；
- 编译器自动识别 slot 是否为指针类型，非指针 store 不插屏障。

#### 5.2.4 根集（精确）

- 编译器为每个函数生成 **stack map**：函数内每个 GC 安全点（分配调用点、函数调用点、
  循环回边）处，哪些栈 slot / 寄存器是指针；
- 全局变量 map：编译期登记全局指针变量地址；
- GC 安全点：编译器在分配、函数调用、循环回边插入安全点检查（对标 Go 的
  `preemptible` 安全点）；
- **不做保守扫描**（0.3 的 PE 数据段/线程栈/进程堆保守扫描全部废弃）。

#### 5.2.5 触发与辅助

- **GOGC 模型**：`next_gc = live_heap + live_heap * GOGC/100`（默认 GOGC=100，即堆翻倍触发）；
- 最小触发阈值（如 4MB）避免小程序频繁 GC；
- **GC assist**：分配时按"本次分配占目标增长的份额"执行相应量的标记工作；
- 后台标记预算：标记线程动态调整以追赶分配速率（对标 Go 的 pacing）。

#### 5.2.6 清扫

- 惰性清扫：分配时按 size class 惰性清扫；后台清扫线程持续清扫；
- 清扫后对象归还 size class 空闲列表 / 归还 OS。

#### 5.2.7 与多线程（spawn/await）整合

- 0.4 支持 `spawn` 真线程（0.3 已具备，0.4 保留）；
- GC 启动时对所有线程：STW（极短）→ 快照各线程栈（精确 stack map）→ 并发标记；
- 写屏障保证标记期间 mutator 修改的指针不丢；
- 线程终止时归还线程本地分配缓存；分配器支持并发无锁/细粒度锁。

#### 5.2.8 finalizer

- 保留 0.3 的 finalizer 能力（`with`/Drop 驱动），但作为可选机制；
- finalizer 独立队列，标记完成后、清扫前执行；finalizer 可复活对象（重新置灰）。

#### 5.2.9 0.3 GC → 0.5 GC 差距对照（逐项消除）

| 0.3 GC | 0.5 GC | 消除方式 |
|--------|--------|----------|
| STW 全停顿 | 并发标记 | 标记线程 + 写屏障 |
| 分代（young/old） | 非分代 | 简化，消除晋升/remembered set 复杂度 |
| 保守扫描兜底 | 精确根集 | 编译器 stack map / 类型表 |
| 无写屏障 | 混合写屏障 | 编译器插入 |
| 手工 root API（frame/root_set/global_root_set） | 编译器自动 root | stack map + 全局 map 自动生成 |
| 自动触发禁用 | GOGC 自动触发 + assist | 运行时 pacing |
| 手工 `lsp_maybe_gc` 调用点 | 自动安全点 | 编译器插入 |
| minor 阈值 256 / major 阈值 1024（魔数） | GOGC 比例 + 最小阈值 | 参数化 |

#### 5.2.10 可观测性

- `GODEBUG` 风格环境变量：`SHADOW_GC_LOG`、`SHADOW_GC_STATS`（对标 `GODEBUG=gctrace=1`）；
- GC 统计输出：各阶段耗时、标记对象数、堆大小、触发原因；
- 压测工具：`test/cases/` 中 GC 压力用例 + 内存峰值断言（见 §5.3 验收）。

### 5.3 GC 验收标准

1. **正确性**：并发标记下无对象泄漏、无悬垂引用（写屏障压测：高频率指针覆盖 + 并发
   spawn/await + 大对象图）；
2. **停顿**：标记阶段用户代码可运行；标记终止停顿可测量且远小于 0.3 全 STW 停顿；
3. **触发**：GOGC 模型下堆峰值 ≈ (1 + GOGC/100) × 存活堆；
4. **长驻**：LSP 服务器连续处理 1000+ 文档编辑，内存稳定无单调增长；
5. **回归**：全部 GC 相关迁移用例（`_gc_` 前缀）全绿。

---

## 6. 运行时设计（最小化，仅系统调用）

### 6.1 分层

```
┌─────────────────────────────────────────────┐
│  Shadow 运行时层（src/runtime/*.shadow）      │  编译为 .o，内嵌进产物
│  string / array / dict / set / any /        │
│  to_string / 数值转换 / 异常标志 / panic     │
├─────────────────────────────────────────────┤
│  C 系统调用层（rt/*.c，0.4 自研）             │  仅操作系统 API
│  alloc / console I/O / file / dir /         │
│  process / time / thread / mutex / args     │
├─────────────────────────────────────────────┤
│  操作系统（Windows kernel32/ntdll；           │
│  Linux libc syscall 封装）                   │
└─────────────────────────────────────────────┘
```

### 6.2 设计约束（参考 Rust）

- Rust 的 `std` 不直接 syscall，而是建立在 libc/OS API 的最小封装上；0.4 同理——
  `rt/` 每个文件对应一组系统 API 的最小封装，**不包含任何第三方库**；
- Windows：直接 `#include <windows.h>`（system DLL），不依赖 MSVCRT 之外的运行库
  （若需要，链接时使用 `/NODEFAULTLIB` 等选项，产物仅依赖系统 DLL）；
- Linux/macOS：仅 libc（glibc/musl），无其他依赖；
- **禁止** STL、`std::string`、`std::vector` 进入产物（这些是 0.3 时代 C++ 运行时的依赖源）；
- 编译器本体（`shadow`）依赖 **LLVM-C.dll**（LLVM-C API）生成 `.ll`，这是工具链的
  必要依赖（原则 3），与产物程序的依赖面严格分离；
- 哈希表（dict/set）：自研开放寻址/链式实现，仅依赖分配器与系统调用；
- 压缩（SPK zip STORE 模式）：`miniz` 源码静态并入工具链，产物程序不链接；
- 线程：Windows `CreateThread` / Linux `pthread_create`（pthread 属 libc，可接受）；
- 进程调用（编译期调用 llc/clang）：Windows `CreateProcess` / POSIX `fork+exec`。

### 6.3 ABI 保持

- 沿用 0.3 的 ABI_SPEC 并修正：`int=i32`、`long=i64`、`double`、`bool=i32(0/1)`、
  `array=[len@0][cap@4][elem_size@8][data@12+]`、`any=[tag@0][value@4]`；
- string 布局变更（长度前缀）属于 ABI 变更，必须提升 ABI version 并提供全部字符串用例迁移（§7）；
- ⚠️ **待办**：codegen 符号 ↔ 运行时符号 ↔ 类型检查器内建的三方一致性目前**没有**自动化校验工具
  （历史文档提到的 `tools/check_runtime_abi.py` 在 0.5 仓库中并不存在）。当前只能靠链接期
  undefined symbol 报错被动发现；建议补一个静态比对脚本。

### 6.4 验收

- 产物 exe 依赖检查：Windows `dumpbin /dependents` 仅系统 DLL；Linux `ldd` 仅 libc
  （`shadow` 本体允许依赖 LLVM-C.dll，属工具链依赖）；
- 最小 hello world 产物体积记录在案，作为回归基准；
- `--exe` 产物可在无 LLVM 安装的干净机器上运行（原则 3 的直接验证）。

---

## 7. 字符串拼接与内存策略

### 7.1 问题定义（0.3 现状）

- 0.3 字符串 = NUL 结尾 C 串（`i8*`）；每次 `+` 拼接 = malloc + memcpy + 释放旧串；
- 链式拼接 `"a" + "b" + ... + "n"` 为 O(n²) 反复分配拷贝；
- ABI 文档承认："编译器内部大量字符串拼接仍可能造成内存膨胀"；
- 编译器自身（lexer/parser/type_checker/codegen）大量使用字符串操作。

### 7.2 0.5 字符串表示

- **长度前缀 + 缓冲区**：`struct ShadowString { len: i64; buf: i8*; }`（内部保持 NUL 结尾
  以兼容系统调用层 `write`/`puts`，但 `len` 是唯一事实来源，杜绝 `strlen` 重复扫描）；
- 短字符串优化（SSO）：长度 ≤ 15 字节直接内联在头部（对标 Rust `String`/Go 无需 SSO 的
  取舍，按 0.5 目标平台选择）；
- 字面量：编译期放入只读数据段，`ShadowString` 头指向只读缓冲，`len` 编译期已知。

### 7.3 拼接策略（对应原则 9）

| 场景 | 策略 |
|------|------|
| 编译期字面量拼接 | **常量折叠**：`"a"+"b"+"c"` 在 parser/优化阶段折叠为单字面量，零运行时开销 |
| 运行期 `s1 + s2`（非原地） | 分配新串（结果长度 = len1+len2 一次算准，只拷贝一次） |
| 运行期 `s += x` / 链式 | **原地追加优化**：若 `s` 唯一引用（无别名）且 cap 足够 → 直接写入缓冲；
  否则收集片段走 `str_join` |
| 编译器内部 / 大量拼接 | **`str_join`**：把片段收集进数组后 `str_join(parts)`——预计算总长度、
  一次性分配结果缓冲、逐段拷贝，无反复分配；编译器内部禁止裸 `+` 链 |
| 字符串字面量 | **intern pool**：编译器内所有字符串字面量去重，相同内容共享地址 |
| 长驻进程（LSP/自举编译器） | 配合 GC（§5）：拼接产生的临时字符串为可回收对象；频繁路径用 arena 批量分配 |
| 数值格式化 | `int_to_str` 等：单次分配，栈上小缓冲（对标 Go `strconv` 的局部缓冲） |

### 7.4 编译器内部纪律（强制）

- `src/` 编译器源码中，禁止用裸 `+` 构造长字符串链；一律使用 `str_join`；
- lexer 累积 token 文本、parser 错误信息、codegen 符号名构造：先把片段收集进数组，
  再 `str_join` 一次性拼接；配合 intern pool 去重；
- 代码评审钩子：grep 检查 `"..." + "..."` 链式模式，超过 2 个连接符必须走 `str_join`。

### 7.5 验收

- 新增压测用例：`//@desc: stress concat 100k`（10 万次拼接），断言正确输出 + 内存峰值
  随次数近似线性（通过 `--run` 下自定义分配统计或外部监控）；
- 自举编译（stage2/stage3）时编译器自身内存峰值记录在案，作为回归基线；
- 全部字符串相关迁移用例（`cg_str_*`、`exa_str_*`、`tc_string*` 等）全绿。

---

## 8. 测试迁移计划

### 8.1 迁移原则

- **语义保留**：用例的输入代码语义与期望输出不变；0.5 语法与 0.3 完全一致（原则 6），
  用例可直接迁移，仅适配工具路径/命名（如 `shadow`）；
- **指令保留**：`//@mode/desc/expect/expect-stderr/skip/xfail/pos/timeout` 指令语义不变
  （`//@expect-ir` 在 0.5 下可恢复严格校验，因为 0.5 的 IR 形状由自己控制）；
- **目录对应**：0.3 用例整体置于 `test/cases/migrated_03/`，保留 0.3 原始目录布局；0.5 原生新增用例直接放在 `test/cases/` 一级目录（见 8.2）；
- **辅助文件**：`.shadow.lu` / `.shadow.sig` 辅助文件随主用例迁移（0.5 的 .lu/.sig 由
  0.5 自身生成，迁移时用 0.5 重新生成）；
- **运行器**：新 `test/run_tests.py` 兼容 0.3 的三种断言形态：
  - rc 判定（tc / tc-fail：0 通过 / 非 0 干净拒绝，崩溃码 139/0xC0000005 单列 CRASH）；
  - .ll 非空（codegen / edge）；
  - exe 运行后 stdout 比对（exe* 系列：子串或严格逐行）；
  - 查询类 stdout 子串（check / ranges / diagjson / typeat）。

### 8.2 目录与用例分布（实测）

> 数据来源：`python test/run_tests.py --list`（2026-08-09 实测）。**实际被运行器收录的用例 = 927 个**。
> 0.3 用例整体迁移到 `test/cases/migrated_03/`，保留 0.3 原始目录布局；0.5 原生新增用例直接放在 `test/cases/` 一级目录。
> 发现规则（`test/run_tests.py:discover`，`run_tests.py:123`）：`os.walk` 遍历，仅当 `.shadow` 文件的**直接父目录名**命中 `FOLDER_MODE`（`run_tests.py:32`），
> 或文件内含 `//@mode:` 指令且该 mode 命中 `FOLDER_MODE` 时，才被收录。

#### 8.2.1 原生 0.5 用例（`test/cases/` 一级）

| 目录 | 用例数 | 断言形态 |
|------|--------|----------|
| check | 5 | stdout 子串（--check） |
| codegen | 19 | .ll 非空 |
| diagjson | 1 | stdout（--diag-json） |
| exe | 24 | exe stdout |
| ranges | 2 | stdout（--ranges） |
| tc | 6 | rc=0（编译通过） |
| tc-fail | 8 | rc≠0 干净拒绝 |
| typeat | 5 | stdout（--type-at） |
| **小计** | **70** | |

注：`test/cases/lsp/`（1 个 `lsp_demo.shadow`，含 `//@mode: lsp`）与 `test/cases/tmp_probe/`（1 个 `probe_smoke.shadow`，无 mode 指令）**不计入** `--list`：
前者由 `test/test_lsp.py` 单独驱动，后者为临时探针、父目录不在 `FOLDER_MODE` 且无 `//@mode`。

#### 8.2.2 迁移自 0.3 的用例（`test/cases/migrated_03/`）

| 0.3 目录 | 用例数 | 断言形态 |
|----------|--------|----------|
| ast | 2 | rc |
| cg | 8 | .ll 非空（经文件内 `//@mode` 回退收录） |
| check | 5 | stdout 子串（--check） |
| codegen | 122 | .ll 非空 |
| diagjson | 1 | stdout（--diag-json） |
| edge | 27 | .ll 非空 |
| exe | 10 | exe stdout |
| exe-ast | 2 | exe stdout |
| exe-codegen | 191 | exe stdout（含 1 个嵌套子用例 `import_priv_call`，靠文件内 `//@mode: exe` 收录） |
| exe-edge | 27 | exe stdout |
| exe-extra | 220 | exe stdout（any/字符串/时间/闭包等） |
| exe-sbg | 1 | exe stdout（SBG 构建） |
| exe-spk | 1 | exe stdout（SPK 依赖） |
| exe-spk-native | 1 | exe stdout（native 链接） |
| exe-tc | 75 | exe stdout（含类型检查） |
| ranges | 2 | stdout（--ranges） |
| tc | 83 | rc=0 |
| tc-fail | 73 | rc≠0 干净拒绝 |
| tc-fail-sbg-version | 1 | rc≠0（版本约束拒绝） |
| typeat | 5 | stdout（--type-at） |
| **小计** | **857** | |

注：
- `migrated_03/exe-sbg-version/`（原 0.3 有 3 例：`pass_build_meta` / `pass_caret` / `pass_prerelease`，每个为 `main.shadow`+`shadow.sbg`+`ver_pkg.spk` 嵌套结构）、
  `migrated_03/simple_test.shadow`（1 例）、`migrated_03/lsp/lsp_demo.shadow`（1 例，含 `//@mode: lsp`）**均未被 `--list` 收录**：
  其 `.shadow` 文件的**叶目录 basename 不在 `FOLDER_MODE`** 且无可命中 `//@mode`，故被 `discover` 跳过。
- 合计：70 + 857 = **927** 个被 `run_tests.py` 发现的用例（与 §3.7 里程碑表一致）。

### 8.3 迁移顺序（与功能开发同步，见 §9）

- 每个 Phase 完成时，迁移该 Phase 相关的用例目录并全绿；
- 迁移完成度 = 0.5 功能完成度的直接度量（原则 5 的验收门槛）。

---

## 9. 完整开发步骤（阶段计划）

> 每一阶段都有明确的交付物与验收标准。**禁止跳过任何阶段**（原则 8）。
> 标注"🔴 自举关键路径"的阶段属于自举链硬依赖，优先保障。

> **Phase 0–16 均已在 0.4/0.5 期间达成**（见各 Phase 的达成记录与 §3.7 债务收口）。
> 保留本节是为了记录实现范围与验收口径，供回归时逐项核对，而非待办清单。

### Phase 0：环境与引导链搭建 ✅
- [x] 冻结 `bootstrap/` 链接期工件（runtime_for_selfhost.o / miniz.o / miniz.h），
      记录 LLVM 22.1.0 路径；
- [x] 创建 `shadow-0.5/` 目录骨架（src/ rt/ test/ tools/ build/）；
- [x] 编写 `build_shadow.sh`：`build/shadow.exe → stage1 → stage2 → stage3 → shadow.exe`
      全链脚本（7 步，见 §4.3）；
- [x] 编写 `tools/verify_fixed_point.py`：stage2.ll 与 stage3.ll 逐字节比对；
- **验收达成**：全链可运行；fixed-point 校验通过。

### Phase 1：语法复刻与 Lexer ✅
- [x] 完成 0.5 词法/语法定义（`kimo`/`dsb`/`import`/`pub` 等全部保留，见 §3.1 清单全覆盖）；
- [x] Lexer：全 token 类型、关键字、数字/字符串/字符字面量、转义、注释、位置区间
      （Location 半开区间模型）、错误恢复；
- [x] 迁移 lexer 相关用例；新增词法错误恢复用例；
- **验收达成**：`--check` 输出正确 token/诊断；词法用例全绿。

### Phase 2：Parser / AST ✅
- [x] AST 结构（Expr/Stmt/Program/FuncDef/StructDef/EnumDef/TraitDef/Type 全节点）；
- [x] Pratt / Precedence Climbing 表达式解析（全部运算符优先级表）；
- [x] 语句解析（let/if/while/for/match/return/break/continue/panic/throw/try-catch/
      with/defer/裸块）；
- [x] 声明解析（泛型函数、泛型 struct/enum、type alias、trait/impl、全局 let、
      extern fn、macro、模块 import）；
- [x] 语法错误恢复（多错误累积、同步恢复）；
- [x] 迁移 `ast`、`parse_*` 相关用例（exe-extra 中的 parse 系列）；
- **验收达成**：parser 用例全绿；`--ranges`/`--at` 输出正确。

### Phase 3：TypeChecker（含泛型单态化）✅
- [x] 类型推断（局部/全局/返回类型）、类型检查规则（算术/比较/逻辑/转型/赋值/下标/
      member/调用/字面量）；
- [x] 泛型：约束（联合约束）、默认参数、可变参数；函数/struct/enum 单态化 + 缓存去重；
- [x] trait/interface：签名兼容、impl 完整性检查、`impl Trait for Type`；
- [x] 可见性检查（pub/私有）、import 符号解析、命名空间隔离（严格态唯一路径，见 §3.8）；
- [x] 结构化诊断：错误码 + file:line:col span（含 LSP 区间）；
- [x] any 类型：box/unbox 规则、隐式转换边界；
- [x] 时间类型（date/timestamp/nanotimestamp）、opaque、future 类型规则；
- [x] 迁移 `tc` / `tc-fail` / `exe-tc` 用例（实测数见 §8.2）；
- **验收达成**：tc 全绿、tc-fail 全绿（干净拒绝 + 错误子串断言）。

### Phase 4：HIR 降级 ✅（2026-08-03 完成）
- [x] HIR 数据结构（HirExpr/HirStmt/HirEnv/HirFunc/HirParam/HirBlock，函数级 slot 空间）；
- [x] 去糖：if/match/for 展开、复合赋值拆解、块扁平化、`&&/||` 短路临时 slot、
      if/match 表达式临时 slot、`?` 错误传播去糖；
- [x] 闭包捕获变量 → children（fd.params 前 captured_count 个，captured_count 从
      e.inferred_type 读取）；range/spawn/await 透传；EXPR_ENUM_VARIANT 模式绑定
      （bind_types 由 TC 回填到 Expr.bind_types 新字段）；
- [ ] 迁移相关用例（cg_* 中依赖 HIR 语义的用例）——待 codegen 完成后随测试迁移；
- **验收**：HIR 输出通过 MIR 下钻验证（实测 hfuncs=2 正确）。

### Phase 5：MIR 降级 ✅（2026-08-03 完成）
- [x] MIR 数据结构（MirInst/MirBlock/MirFunc/MirParam，29 种 MIR_OP）；
- [x] 控制流线性化（基本块划分、loop break/continue 栈、for → while + 隐藏索引变量）；
- [x] `.lu` 序列化/反序列化（v8：LEB128 + raw 8B，globals 含常量初始值 LU_INIT_*、
      pub/ext 标志），原子写入（.tmp + rename）；实测往返一致（17 blocks/77 regs 不变）；
- [ ] GC 相关：MIR 层标记指针 slot（供 §8 GC 的 stack map 生成）——并入 GC v1；
- **验收**：`--mir-cache` 路径与普通路径测试结果一致（序列化往返已验证）；
  MIR 指令级验证：ENUM_CONSTRUCT/GET_TAG/GET_PAYLOAD/MAKE_CLOSURE/CALL_INDIRECT/
  CONDBR/rt_throw_str 计数与预期一致。

### Phase 6：LLVM 代码生成 + 链接 → shadow ✅
- [x] LLVM-C API 桥接层（`shadow_llvm_*`，线程局部 buffer 解决数组参数）；
- [x] 全表达式/语句 → LLVM IR 生成（含闭包、enum 构造/解构、any box/unbox、
      spawn/await、字符串操作、GC 分配调用点）；
- [x] 字符串全局 dedup、函数去重（最后一次胜出）、main 追加 (argc, argv)；
- [x] `llvm_print_mod` 输出 .ll；`llc` → `.o`；链接 rt/ 运行时 `.o` + **LLVM-C.dll**
      （编译器工具链依赖，原则 3）→ `shadow.exe`；产物程序仅系统库；
- [x] `--run` 路径（编译 + 链接 + 运行）；~~`--emit-runtime-cache` / `--runtime-cache`~~
      （0.5 架构下无独立运行时编译单元，已废弃并从构建脚本移除，2026-08-05）；
- [x] 迁移 `codegen`（122）/`edge`（27）/`exe`（10）/`exe-codegen`/`exe-edge`（27）用例；
- [ ] **待办**：codegen 符号 ↔ 运行时符号 ↔ 类型检查器内建三方一致性的自动化校验脚本（当前缺失）；
- **验收**：exe 系列用例全绿；产物依赖检查通过（仅系统 DLL）。

### Phase 7：字符串系统（原则 9）✅（保留 2 项自举后优化，见下）
- [x] **runtime 层 str_join 等全套实现**（2026-08-03）：runtime_lib.shadow 新增 shadow_join/
      shadow_contains/shadow_index_of/shadow_substr/shadow_char_at/shadow_subscript/shadow_str_replace/
      shadow_trim/shadow_to_upper/shadow_to_lower/shadow_split/shadow_format（0.5 专属命名，避开与
      runtime_for_selfhost.o 的 C++ shadow_string_* 重复定义）；str_join 预计算总长一次分配；
- [x] **codegen 映射补全**：cg_resolve_extern_name 字符串函数全套映射到 0.5 命名；字符串下标改
      shadow_subscript；TC 修 str_split 返回类型（TYPE_STRING→TYPE_ARRAY）；
- [x] **编译器内部纪律**：main.shadow 的 import 递归加载 + runtime 注入已用 str_join/substr2/
      str_trim/str_split 实现（替代裸 + 链）；其余裸 + 链随 Phase 15 迁移统一清理；
- [ ] `ShadowString`（长度前缀 + 缓冲）表示与 ABI 落地（**推迟到自举后 §12 F6**：0.3 语法/ABI 为
      NUL 结尾 C 串，改动动 ABI 与全部字符串用例，先保持 0.3 兼容）；
- [ ] 编译期字面量常量折叠；intern pool 去重（自举后优化项）；
- [x] 字符串全套内置（str_eq/contains/index_of/replace/trim/to_upper/to_lower/char_at/
      split/join/format、substr、len、下标）——0.5 runtime 层已实现；
- [x] 迁移 `cg_str_*`、`cg_string_*` 核心 codegen 用例（15 个，expect-ir 适配 0.5 符号）；
      `exa_str*`、`tc_string*` 随 Phase 15 批量迁移；
- [x] 新增 10 万次拼接压测用例（ex_str_stress，str_join 收集模式，实测 75ms）；
- [x] **验收达成（2026-08-04）**：字符串用例全绿（run_tests PASS=27 FAIL=0）+ 压测 75ms 近线性 +
      自举链 fixed-point（stage2.ll == stage3.ll）。

### Phase 8：运行时系统调用层（原则 3）
- [x] **`rt/` 纯 C 系统调用层落地**（2026-08-03）：rt_core.c（size-class freelist 分配器 +
      基础原语）、rt_fs.c（file/dir/path）、rt_proc.c（CreateProcess 管道 sys_exec/env/exit）、
      rt_time.c（date/timestamp/nanotimestamp + 格式化）、rt_err.c（异常标志/panic）、
      rt_extra.c（链式哈希 dict/set/hashmap）；全部 clang 编译通过，build/rt/*.o 就绪；
- [x] 内存分配器第一版（size-class freelist：16..8192 共 19 级 + 大对象独立提交；线程本地缓存
      留待 Phase 9 GC 整合）；
- [x] Shadow 运行时层：array/dict/set/any/to_string/异常/panic/sys_args（runtime_lib.shadow）；
- [x] 哈希表自研实现（dict/set：链式 + FNV-1a 哈希）；
- [x] 用户产物链接切换：runtime_for_selfhost.o → rt 层（tools/link_rt.py + 手工验证；
      main_link_exe 切换待 --run 全链路落地后收口——当前 --run 仍链 runtime_for_selfhost.o，
      但产物程序已验证可脱离 C++ runtime 独立运行）；
- [x] 迁移 exe-extra 中 I/O/时间/进程相关用例（path_*、file_*、dir_*、time_to_str）（随 Phase 15 完成）；
- [x] **验收达成（2026-08-04）**：字符串用例全绿；产物（rt 层链接）依赖检查通过
      （仅系统 DLL；tools/link_rt.py 链接的 exe 不依赖 C++ runtime）。

### Phase 9：GC v1（STW 精确标记-清扫）✅
- [x] 对象头 + 类型表（精确指针追踪）；size-class 分配器 + 线程本地缓存；
      —— 对象头复用分配器块头（RT_HEADER 16：`[size][type_id][mark][pad]`）；
      对象登记表（数组+空闲链）+ ptr→idx hash set；类型表内置
      DATA/STRING/ARRAY/ANYBOX/DICT/CLOSURE + 编译器 register_type(id,size,bitmap)；
- [ ] 编译器生成 stack map / 全局变量 map（安全点插入）；
      —— v1 用 slot 键控 root_set（精确）+ 保守栈扫描兜底（TEB 逐页已提交非 guard）；
      stack map 留待 Phase 10；
- [x] 三色标记器（STW 版，先保证正确性）；
- [x] 惰性清扫 + 归还 OS（sweep 未标记 → free 进 freelist；GOGC 触发）；
- [x] `SHADOW_GC_LOG` 观测（[GC] start/done freed/live/next + reg/unreg 计数）；
      `SHADOW_GOGC`/`SHADOW_GC_FORCE`/`SHADOW_GC_DEBUG` 环境变量；
- [ ] 迁移全部 `_gc_` 前缀用例（0.3 的 tc_gc_*/tcf_gc_* 随 Phase 15 批量迁移）；
- [x] GC 压测用例（ex_gc_cycle 循环引用 / ex_gc_stress 大对象图 / ex_gc_strings /
      ex_gc_finalizer Drop 回收 / ex_gc_any any 保活）；
- **验收**：GC 正确性用例全绿（5/5）；字符串 8/8 回归；自举链 fixed-point 达成；
      产物链接切 rt 层（--run 独立运行）；LSP 长驻留待 Phase 14 验证。
- **v1 关键决策**：array 保持 2 参布局（s03 宿主 ABI 约束），元素指针性用
      "elem_size==8 保守逐元素查对象表"（无 flags）；root_set 必须 slot 键控 replace；
      any 运行时是裸指针（as any 转换才走 AnyBox 盒）；编译器本体缺
      shadow_gc_register_type → rt/shadow_gc_supplement.c no-op 补充。

### Phase 10：GC v2（对标 Go：并发标记 + 写屏障）✅（2026-08-04 完成，commit 09db980 等）
- [x] 编译器插入混合写屏障（插入 + 删除），非指针 store 不插；
      —— codegen 已插 2 参写屏障（MIR_STORE_INDEX/MEMBER 指针 store）；
      rt_gc.c 已实现插入屏障（marking 时新值置灰）+ root_set 屏障；
- [x] 并发标记 worker 线程（并行标记 + 全局/本地灰队列）；
      —— 全局队列 + 锁的 work stealing 简化（§5.2.2 允许）；N=min(核数,8) 常驻线程，
      SHADOW_GC_MARKWORKERS 覆盖（0=关）；
- [x] GOGC 触发模型（堆增长比例 + 最小阈值）+ GC assist（分配时协助标记）——§5.2.5（13ec39f）；
- [x] 极短 STW 标记终止（协作式安全点）——§5.2.4 完整版：poll 插入 + gc_state 状态机，
      标记终止对标 Go mark termination（5d31d2c + 09db980）；
- [x] 多线程整合：spawn/await 线程精确根集 —— alloc_root 分配根 + sf/slot/range 根
      （bfdf400 精确根集 + 09db980 alloc_root，对标 Go allocate-black）；
- [x] 压测：高频率指针覆盖 + 并发 spawn/await + 大对象图（ex_gc_barrier/ex_gc_spawn/
      ex_gc_stress/ex_gc_mixed/ex_gc_cycle），STRESS+VERIFY=2 missed=0，无泄漏无悬挂；
- [x] `SHADOW_GC_LOG` 输出对标 `GODEBUG=gctrace=1` 格式（§5.2.10）；
- **推进记录**：早期探索受阻（ex_gc_strings 崩，回退 v1）后，经 §5.2.4 精确根集 stack map
      （bfdf400）、协作式安全点基础设施（5d31d2c）、并发标记+后台清扫+协作 STW 完整切换
      （09db980）三个阶段全部攻破；核心修复 = alloc_root 分配根（分配路径 C 中间帧无根窗口）。
- **验收达成**：§5.3 全部标准（正确性/停顿/触发/长驻/回归）；37/37 用例全绿；
      保守开/关对拍一致（精确根集完备，无保守扫描兜底）。

### Phase 11：模块系统（§3.8 新机制）+ 缓存 ✅（0.5 严格态唯一路径收口）
- [x] **模块/包模型**：shadow.sbg 清单解析（[package] name/version/abi_version + [deps]
      name="path" 目录型，TOML 微子集）；主模块根 = 主文件目录；shadow.lock 随 Phase 12
      semver 落地；
- [x] **import 解析（§3.8.3 完整实现，main.shadow 加载层）**：**点号=目录**（Java 模型，
      2026-08-06 修订，推翻原"首段=模块名"Go 模型）；`a.b.c` → 源码根 `a/b/c.shadow`；
      ① 主模块自身（本地嵌套目录解析，自举前提 B 通过：core/lexer/parser 等）；
      ② [deps] 目录依赖（首段作外部依赖名，dep.path 目录下解析）；③ 失败报
      "no required module provides package"（对标 Java package not found）。单段 import 支持文件与目录=包；
- [x] **包级 DAG 循环检测**：main_path_stack（DFS 路径 push/pop）+ main_compiled（只加不减）
      两结构分离 —— **菱形依赖合法**（实测 4 用例全过）；**循环 import 放开**（Java 规则，
      a↔b 互引合法；0.5 删除兼容态后 `circular import detected` 一并移除）；
- [x] **目录=包**：<pkg>.shadow 缺失时加载 <pkg>/ 下全部 .shadow（**不递归**子目录，Java 通配符规则），
      插入排序保证确定性（FindFirstFile 顺序不定，排序防 fixed-point 漂移）；
- [x] 可见性：`pub` 跨包导出检查已落地 —— `tc_mg_lookup_*` 五处（struct/enum/alias/func/trait）
      带 `gres.is_pub == 1` 过滤，同包私有可见由 `tc_same_pkg` 放宽为 `is_pub || 同包`；
      import 仍只接受 pub（详见 §3.8.2）；
- [x] `.lu` 函数级增量缓存 —— 每模块独立 `.lu`（缓存键 = 路径 djb2 + 源码 hash + 世界签名 WSIG），
      命中则跳过该模块的 TC 函数体检查与 hir/mir 降级；2026-08-09 债务③ 收口后**默认开启**
      （`main_g_lu_cache = 1`），自编译 322s → 117s。**无需任何 CLI flag**（历史文档提到的
      `--mir-cache` 从未实现）。见 §3.7 债务③ 收口记录；
- [x] 新增模块用例：ex_mod_pkg（主模块子包，exe 运行验证 42）、tc_mod_deps（sbg deps
      目录依赖）、tcf_mod_circular（循环报错 tc-fail）、tc_mod_diamond（菱形合法）；
- **验收**：模块用例全绿；import 解析覆盖 主模块/目录依赖 两路径（SPK 依赖随 Phase 12）。

#### §3.8 补丁 A：命名空间 + FQN 解析基础设施（2026-08-05 落地，现服务于严格态）

**目标**：落地与 Java 一致的「命名空间 + import」语义所需的**命名空间 + FQN 解析基础设施**；
该基础设施现服务于严格态路径（按所属模块 mangle、package-private 可见性）。原「全局 `pub`
可见 / 扁平编译」的兼容态行为已随 0.5 删除兼容态而移除。

- **命名空间派生自文件路径**：`core\ast.shadow` → `core.ast`；runtime 固定为
  `shadow.runtime`（`main_key_to_ns`）。无需在源码里写 `package` 声明。
- **行范围归因**：combined 源是所有模块按行拼接的单一文本，故用
  `main_record_mod(ns)` 在追加每个模块前记录其起始行（`len(main_parts)+1`，
  main_parts 逐行存储），TC 侧 `tc_module_of_line(line)` 反查某声明归属哪个命名空间。
- **状态注入**：`tc_set_mod_map(ns, starts)` 写入 **type_checker 模块内部全局**
  （`tc_g_mod_ns` / `tc_g_mod_starts`），由 `main_load_combined` 末尾调用。
  ⚠️ 不可放在 main.shadow 的全局里——main 的全局对 `api`/`lsp` 模块不可见；
  放 TC 内还让 `tc_check_program(prog)` 保持**单参签名**，compile_api / LSP 调用点零改动。
  未注入时两数组为空 → `tc_is_known_ns` 恒假 → FQN 解析自动降级关闭。
- **数据结构**（`CompileContext` 新增 11 字段）：`mod_ns` / `mod_starts` +
  `ns_keys`（`"ns::Symbol"`）/ `ns_kinds`（1=struct 2=enum 3=func 4=alias 5=trait 6=global）/
  `ns_idx`（在对应 kind 分数组中的下标）+ 6 个分数组。
  **`ns_idx` 必须存在**：`ns_keys` 是混排的，用它的下标去索引 `ns_structs` 等分数组会错位。
- **仅 pub 项**进入命名空间树（`tc_build_ns_tree`，Pass1 后调用）。
- **叠加层触发条件**：只有符号名**含点号**时才走 FQN 路径；裸名一律沿用原有全局 pub 回退。
  编译器自身源码不写点号限定名 → 该层对自举产物零影响。
- **覆盖点**：6 个 `tc_mg_lookup_*` 前置 FQN 分支；`tc_check_member`
  （模块限定函数 → 改写 `EXPR_CLOSURE`，模块限定全局 → 改写 `EXPR_VAR`）；
  `tc_check_call`（`parse_member_chain` 把 `a.b.c.f(x)` desugar 成 `f(a.b.c, x)`，
  识别首实参为已登记命名空间后剥离伪 receiver）；`tc_resolve_type_name` /
  `tc_resolve_type`（FQN 类型名归一为裸名后重入）。
- **遮蔽规则（Java 语义）**：局部变量遮蔽包名——点链根段若已绑定为变量，不走 FQN 路径。
- **parser**：`parse_type` 支持点号限定类型名 `a.b.C`；`import a.b.*` 解析为等价的
  `import a.b`，由既有「目录=包」逻辑展开目录下全部 `.shadow`。
  ⚠️ **通配符必须在两处分别处理**：parser 的 `parse_program` import 分支（保证语法不报错），
  以及 `main_import_target`（**模块加载真正的入口**）。后者是**文本级扫描**——直接取
  `import ` 与 `;` 之间的原文当模块路径，**不经过 parser 的 `prog.imports` 数组**。
  只改 parser 会得到 `no required module provides package "nswild.*"`。
- **新增用例**：`ex_ns_fqn`（FQN 类型 + FQN 调用 + 裸名共存）、
  `ex_ns_wildcard`（通配符导入 + 跨文件 FQN）。
- **严格态（0.5 唯一路径）**：私有符号跨包不可见、`pub` 跨包导出检查、同名符号跨命名空间共存
  已由 `tc_g_pub_conflicts` 实现（使用点报 `ambiguous`，不再撞名）；符号按所属模块 mangle，跨模块同名可共存。

##### 与 Java import 的语义差异（0.5 严格态实测核对）

0.5 **仅保留严格态**（已删除兼容态）；严格态底层是「符号引用 + 模块命名空间」而非兼容态的
「源码物理拼接」。对照 Java，四条硬差异在严格态下**仅剩第 4 条**：

| # | 场景 | Java | Shadow 0.5 严格态 |
|---|---|---|---|
| 1 | **传递性** | 不传递。A import B、B import C，A 里**不能**用 C 的简名 | **不传递**（一致）。`tc_visible_from` 只认当前模块**显式 import** 的模块（`type_checker.shadow:1245` 的 `tc_g_mod_imp_list`），B 的 import 不会传递给 A；A 需用 C 的 FQN 显式引用 |
| 2 | **同名符号** | 两包可共存同名类；仅在简名使用点报 ambiguous，可用 FQN 消歧 | **一致**：跨模块同名 `pub` 由 `tc_g_pub_conflicts` 记录，使用点报 `ambiguous` 而非静默取第一个（`type_checker.shadow:496`、`tc_pub_conflict_*`） |
| 3 | **循环 import** | 允许（类之间可互相引用） | **允许**（一致）。严格态下 DFS 命中路径栈直接视为已处理返回成功，不再报 `circular import`（`main.shadow:764`） |
| 4 | **导入粒度** | 导入**类型**（`import a.b.C` 的 C 是类名） | 仍导入**文件/目录**（`import a.b.c` 的 c 是文件名）；无单类型导入——这是「目录=包」模型的固有粒度，**结构性差异保留** |

另有两点对齐/差异：
- **package-private 已具备**：同目录（同包）未导出符号互相可见（`tc_same_pkg` / `tc_visible_from` 的 same-pkg 分支，`type_checker.shadow:1243`），比兼容态的「pub 全局可见」更接近 Java；跨包仍仅 `pub` 可见、私有不可见。
- **命名空间由文件路径派生**（`dsb` 声明不参与），而 Java 由 `package` 声明决定并校验目录一致——这一差异保留。

> 结论：严格态已消除差异 1/2/3（与 Java 在传递性、同名歧义、循环 import 上行为一致），
> 仅差异 4（导入粒度）和「命名空间=文件路径」模型是 0.5 与 Java 的结构性区别。

### Phase 12：SBG / semver / SPK（§3.8.4 新机制）✅ 核心完成（2026-08-04，commit 5af416a）
- [x] SBG：TOML 微子集解析（`[package] name/version/abi_version` + `[deps] name="path"`
      与 `[deps.<name>]` 表 path/version）；shadow.lock lockfile（解析后写入）；
      传递依赖递归加载 = SPK 缓存内模块的 import 递归（走 §3.8.3，循环防护复用
      path_stack/compiled_set）——缓存目录 shadow.sbg 的 deps 表加载列为后续细化；
- [x] semver 2.0.0 完整实现（src/core/semver.shadow：sv_parse/sv_cmp/sv_match，
      约束 `*/^/>=/<=/>/</=/区间`，prerelease 逐标识符比较；b77e451 完成）；
- [x] **SPK 版本化全局缓存**：`$SHADOW_HOME` 或 `%USERPROFILE%\.shadow\pkg\mod\<module>@<version>/`
      解包（幂等复用），content_hash（FNV-1a 64）+ version 约束校验、版本隔离不覆盖
      （对标 GOMODCACHE/Cargo registry）；
- [x] SPK：pack/unpack/list（miniz STORE zip，bootstrap/miniz.o+miniz.h 静态并入）、
      `shadow pack/unpack/list/-init` CLI；native 文件收集链接、ABI sentinel 留后续；
- [x] `-init` 脚手架（生成 shadow.sbg 模板）；
- [ ] 迁移 `exe-sbg`/`exe-sbg-version`/`exe-spk`/`exe-spk-native`/`tc-fail-sbg-version` 全部用例
      （含 `pass_build_meta`/`pass_caret`/`pass_prerelease`；用例文件在 0.3，随 Phase 15 批量迁移）；
- **验收达成**：SPK e2e（pack → deps .spk → 缓存 → import → 编译运行 42）+ 幂等二次编译；
      content_hash/版本约束校验通过；42/42 用例全绿；fixed-point PASS（7127290B）。

### Phase 13：协程与并发原语 ✅（2026-08-04 完成，commit c95e633 + 09db980）
- [x] spawn/await（真线程 + Future 句柄）、闭包支持（rt_thread.c：ShadowTask/ShadowFuture/
      条件变量，spawn 闭包 env 传递；codegen MIR_SPAWN/MIR_AWAIT）；
- [x] 与 GC v2 整合（协作安全点 + alloc_root 分配根 + 各线程帧根扫描 + 分配器并发安全
      mspan 协议修复，对标 Go mcache/mcentral）；
- [x] 迁移 `cg_*spawn*`/`await`/闭包相关用例（ex_gc_spawn 多线程 STRESS+VERIFY 压测全绿）；
- **验收达成**：并发用例全绿；GC 压测在并发下通过（37/37，ex_gc_spawn STRESS=64 VERIFY=1 PASS）。

### Phase 14：LSP 服务器（117 项功能全复刻）
- [ ] JSON-RPC over stdio 协议层（Content-Length 分帧、请求/响应/通知分发、取消）；
- [ ] 编译器即库 API（compile_api：check/type_at_pos/node_at_pos/find_definition/
      find_references/collect_completions/hover/sdiag_json_array）；
- [ ] 16 类 117 项功能逐项实现（§3.6 表）：补全/悬停/签名/跳转/引用/重命名/代码操作/
      格式化/诊断/语义着色/内联/层次/符号/颜色/文件操作/工作区进度；
- [ ] 文件监听（原生目录监控 + mtime 轮询）、多根工作区、配置同步、进度报告；
- [ ] SPK 包内 import 解析（幂等解包缓存）；
- [ ] 迁移 `lsp`/`check`/`ranges`/`diagjson`/`typeat` 用例 + 新增 LSP 专项测试
      （参照 0.3 的 test_lsp.py 体系）；
- **验收**：LSP 用例全绿；`--lsp` 下 117 项功能可演示。

### Phase 15：测试全量迁移收口 + 回归 ✅
- [x] 用例全部迁移完毕（`test/cases/migrated_03/` + 0.5 新增目录），逐目录核对 §8.2 表；
- [x] 全量回归：`python test/run_tests.py -j 8` → PASS=927 / FAIL=0 / CRASH=0；
- [x] LSP e2e：`python test/test_lsp.py` → PASS=5/5（含 multimodule 跨模块严格态场景）；
- [x] 产物依赖检查与产物体积记录（`build/shadow.exe` 1,754,112 B）；
- **验收达成**：全量测试全绿 = 功能复刻完成（原则 1 + 原则 5 达标）。

### Phase 16：自举验收 ✅
- [x] stage1：冷启动宿主 `build/shadow.exe` 编译 0.5 源码 → `shadow-stage1.exe`；
- [x] stage2：stage1 编译器编译同一份源码 → `shadow-stage2.exe`；
- [x] stage3：stage2 编译器编译同一份源码 → `stage3.ll`；
- [x] fixed-point 校验：`stage2.ll == stage3.ll` 逐字节相等
      （`tools/verify_fixed_point.py`；当前 11,031,861 B，stage1 亦与之相等）；
- [x] 命名收口：产物统一 `shadow`（`build/shadow.exe`），无历史 s0x 命名；
- **验收达成**：`shadow` 可编译自身源码并达成 fixed-point；`shadow --version` 输出 `shadow 0.5`；
      自举链经 `build_shadow.sh` 三阶段全量重建验证。

### Phase 17：发布与配套
- [ ] 打包发布：`shadow` 单一可执行文件 + 文档（README/架构/功能清单/ABI 规范）；
- [ ] VSCode 扩展配套（shadow-vscode 风格）：compilerPath 指向 `shadow`，LSP 对接；
- [ ] GC 调优文档（对标 Go 的 GOGC 说明）；
- [ ] 性能基线：编译自举源码耗时、产物体积、GC 停顿指标记录；
- **验收**：发布物齐全，文档与实现一致。

### 里程碑总结

| 里程碑 | 阶段 | 标志 |
|--------|------|------|
| M1 引导链跑通 | Phase 0 | 最小自举链 + fixed-point 脚本 |
| M2 前端完整 | Phase 1-3 | lexer/parser/TC 全绿（tc/tc-fail/exe-tc） |
| M3 中后端完整 | Phase 4-6 | exe 系列全绿，产物仅系统依赖 |
| M4 运行时完整 | Phase 7-8 | 字符串压测 + 运行时用例全绿 |
| M5 GC 对标 Go | Phase 9-10 | §5.3 验收标准全部达成 |
| M6 生态完整 | Phase 11-14 | SBG/SPK/semver/LSP 全绿 |
| M7 全量回归 | Phase 15 | 927 用例全绿 ✅ |
| M8 自举达成 | Phase 16 | stage2.ll == stage3.ll ✅ |
| M9 发布 | Phase 17 | shadow 单一可执行发布（进行中） |

---

## 10. 禁止事项

- ❌ 禁止省略 §3 清单中的任何功能或任何语法特性（含 `kimo`/`dsb`，原则 1/原则 6）。
- ❌ 禁止跳过 HIR/MIR 阶段直接生成 LLVM IR（原则 8）。
- ❌ 禁止产物程序链接 C++ 运行时 / 第三方库（原则 3）；LLVM-C.dll 仅作为编译器
  工具链依赖，禁止其进入产物程序。
- ❌ 禁止以 `s0x` 命名产物（原则 4）。
- ❌ 禁止在未做自举对照实验的情况下判定改动"没有回归"（§4.4）。
- ❌ 禁止把 `rt_io.o` 加入 `shadow.exe` 链接行（duplicate symbol，§4.2）。
- ❌ 禁止恢复兼容态（物理拼接 / `ctx.strict` 分叉）——严格态是唯一路径（原则 1）。
- ❌ 禁止编译器内部用裸 `+` 链式拼接（必须走 `str_join`，原则 9 / §7.4）。
- ❌ 禁止照搬 0.3 的 import 解析机制（"相对主文件目录"语义）——0.5 必须用 §3.8 的
  模块/包/SPK 新机制（用户明确要求）。
- ❌ 禁止保守式 GC 扫描（PE 数据段/线程栈/进程堆）回归（原则 7，§5.2.4）。
- ❌ 禁止静默丢弃测试用例（原则 5）。

## 11. 风险登记

| 风险 | 等级 | 缓解 |
|------|------|------|
| 冷启动宿主（build/shadow.exe）潜在 miscompile | 高 | stage1 后立即接入 fixed-point 逐字节校验 |
| 语法演进破坏 fixed-point | 高 | 语法以复刻 0.3 为基线，新增特性以 stage 为单位推进，每次演进后重放 stage1→stage3 |
| GC 并发标记正确性 | 高 | 写屏障压测 + 并发协程压测（§5.3） |
| 字符串 ABI 变更破坏兼容 | 中 | ABI version 提升 + 全部字符串用例迁移（§7） |
| LLVM 版本漂移 | 中 | bootstrap 固定 LLVM 22.1.0 路径，升级需全量重放 |
| 泛型边界（复杂约束/嵌套） | 中 | 先保守使用，按用例逐步放开 |
| LSP 长驻内存 | 中 | GC v2 完成后接入 LSP 长驻压测 |

---

## 12. 自举成功后修复清单（future work）

> 下列为 0.5 自举期源码层**遗留限制**（原 0.3 宿主 s03.exe 的规避写法，部分已在 0.5 自举后消除）。
> 0.5 源码当前仍可能需规避（见 core/utils.shadow 头部规范与记忆），**fixed-point 达成后**
> 由 0.5 编译器自身实现修复——届时 0.5 源码可逐步放弃规避写法，改用自然语义。
> 每项修复后重放 stage1→stage3 并全量回归（`test/run_tests.py`）。

| # | 宿主限制（当前规避方式） | 自举后目标实现 |
|---|--------------------------|----------------|
| F1 | **函数级作用域**：同一函数内同名 `let` 类型必须一致（如 string `msg` 与 Expr `msg` 冲突报错）→ 当前靠命名避让（`panic_msg`/`tup_idx` 等） | 0.5 实现**块级作用域**：不同块/分支内同名变量允许不同类型，类型按声明处局部推断（**待验证是否已在 0.5 落地**） |
| F2 | ~~`arr[i].field` 连写不被支持（`expected ; but got '.'`）~~ | ✅ **2026-08-09 已实现**（见下方「F2/F3 收口记录」） |
| F3 | ~~`(expr as T).field` 连写不被支持~~ | ✅ **2026-08-09 已实现**（同上） |
| F4 | 无 `.lu` 缓存时首次编译大模块（parser 900KB+ .ll）耗时数分钟（冷缓存 ~315s，见 §3.7） | 0.5 增量编译 + 分模块并行 codegen + 稳定的 `.lu`/`.sig` 缓存（Phase 11 已落地），热缓存首编降至 ~117s |
| F5 | 字符串插值测试探针中 `${...}` 会被当作插值（0.3 语义） | 0.5 保留该语义（语法复刻），但需提供转义 `\${` 的明确行为（§7 字符串系统设计时定义）（**待验证**） |
| F6 | 字符串为 NUL 结尾 C 串（`ShadowString` 未落地），改动字符串 ABI 会波及全部字符串用例 | 0.5 落地 `ShadowString`（长度前缀 + 缓冲）表示与 ABI（Phase 7 推迟项），intern pool 去重 |

> 落地节奏：F1–F3 属语义/语法增强，在 fixed-point 达成后随"0.5 源码语法演进"一起做
> （§4.4 自举验证纪律，stage 为单位重放）；F4 属工程性能，在 Phase 11 模块缓存设计时就
> 以 0.5 自身编译器为目标实现；F5 随 §7 字符串系统定稿；F6 随 Phase 7 收口。

### F2/F3 收口记录（2026-08-09）

**根因**：`parser.shadow` 里两条主表达式分支在构造完节点后**直接 `return`**，跳过了后缀成员链解析：
- 索引分支（IDENT 后接 `[`，约 :963）构造 `EXPR_INDEX` 后 `return e;`
- 括号分支（`( expr )`，约 :994）`ps_expect(RPAREN)` 后 `return first;`

**修复**：两处改为 `return parse_member_chain(<node>, loc);`。

`parse_member_chain(base, loc)`（:1030）的结构是 `while(true){ if(TOK_DOT){…} else if(TOK_LBRACKET){…} else { break; } }`
——**下一个 token 不是 `.` 或 `[` 时原样返回 base**，因此对所有既有写法是**严格 no-op**，
这正是它能零回归接入的原因（也是选择"包一层"而非"改语法结构"的依据）。

覆盖的连写形式（新增用例 `test/cases/exe/ex_member_chain_index.shadow` / `ex_member_chain_paren.shadow`）：

| 形式 | 例子 |
|---|---|
| 索引后取字段 | `arr[i].x` |
| 索引后调方法 | `arr[i].mag()` |
| 多级索引 + 字段 | `rows[0].cells[1].x` |
| cast 后取字段/方法 | `(a as P).x`、`(a as P).mag()` |
| 函数调用结果取字段 | `(mk(4,7)).x` |
| 括号表达式再索引 | `(nums)[0]` |

**验证**：`d2_stage2.ll == d2_stage3.ll == 10,981,290 B`（字节相同，fixed-point）；
相比债务① 基线 10,980,114 B 仅 **+1,176 B**（两处新增调用点，量级吻合）；
全量 `PASS=925 FAIL=0 CRASH=0 SKIP=2 TOTAL=927`（923 → 925 恰为新增两例，零回归）。

**方法学**：改前用 `build/_probe_f23b.shadow` 在旧 exe 上复现 `SH-PARSE002 expected ; but got '.'`
确认债务真实存在，并用中间变量等价写法（`_probe_f23.shadow`）先量出**期望输出**作为断言基准；
改后同一探针在新 exe 上输出与基准一致 → 证明是"新增能力"而非"改变既有语义"。

---

## §3.9 Phase 15 严格态：真·分模块编译单元（2026-08-05 立项）

### 目标
消除 §3.8 严格态对照表中的 4 条硬差异（传递性 / 同名符号 / 循环 import / 粒度）中的
**传递性 + 同名符号 + 循环 import** 三条（原 Phase 15 计划聚焦前两条，循环 import 放开随严格态一并落地），仅**粒度（#4）**保留，实现 Java 式命名空间隔离：
- **同名符号跨命名空间共存**：`a.c()` 与 `b.c()` 可同时存在于一个程序，互不影响。
- **私有符号跨包不可见**：非 `pub` 符号**同包（同目录）可见（package-private）**，跨包不可见
  （符号按完整模块路径 mangle 隔离，文件级唯一）。
- **pub 导出检查**：被跨模块引用的符号必须 `pub`，否则报未导出。
- **import 可见性**：调用方必须 `import` 了符号所属模块（或写 FQN）才能用其 pub 符号。
- **歧义报错**：简名命中 2+ 绑定报 `ambiguous reference`。
- **简名只在「本命名空间 + 同包 + 显式 import 列表」内解析**（不再全局 pub 回退）。

### 策略（已决策：真·分模块 + 链接期合并，零宿主改动）
用户场景：`a` 有 `c()`、`b` 有 `c()`，`d` import `a` 后用 `a.c`、`e` import `b` 后用 `b.c`，
且不想总写全路径 → 严格态下 `d` 经 `import a` 把 `a` 的 pub 简名带入 `d` 作用域，
`c` 解析到 `a::c`；`e` 同理解析到 `b::c`；二者在二进制中并存。

**关键技术前提（已验证）**：codegen 走的是**真实 LLVM 内存 IR**
（`LLVMAddFunction`/`LLVMBuildAdd`/`LLVMPrintModuleToString` 等真·C API，非手工拼文本），
`LLVMLinkModules2(LLVMModuleRef Dest, LLVMModuleRef Src)` 在 `llvm-c/Linker.h` 与
`LLVM-C.dll` 中真实存在。Shadow 用 `@extern` 直接绑 LLVM-C 符号，**无需改 s03 宿主、无需重编宿主**。

### 架构现状（改造落点）
1. **加载**：`main_load_combined`（main.shadow:577）DFS 文本拼接成单一源码 → 改产出模块列表。
2. **TC**：`tc_check_program`（type_checker.shadow:3958）仅建**单** ModuleNode → 改逐模块建节点入共享 graph；
   `tc_mg_lookup_*`（211/239/265/291/317/362）已是「本模块私有优先 + 全局 pub 回退」骨架，在此补可见性/歧义。
3. **codegen**：`cg_gen_program`（codegen.shadow:2372）单 LLVM module → 已拆为 `cg_gen_module(prog,mfuncs,ctx,mod)`
   + 兼容包装 `cg_gen_program` + 多模块入口 `cg_gen_program_multi`；新增 `cg_link_modules` 走 `LLVMLinkModules2`。
4. **符号名**：模块自有符号 IR 名加模块前缀（复用 `tc_mangle_private` 风格 `__mod_<mod>__<name>`），
   extern/runtime（`rt_*`/`shadow_*`）保持原名 ExternalLinkage 不动。
5. **自举不变**：stage1→3 仍 `exe src/main.shadow -o stageN.ll`；`main.shadow` 已是「集中 import 全部编译器模块」入口，
   严格态下其引用符号只要 `pub` 且被 import 即可。

### 里程碑
| M | 内容 | 验证门 |
|---|---|---|
| M0 | 加载层：模块列表替代物理拼接（runtime 不再文本拼接，作依赖模块之一） | 现有 57/57 不退化 |
| M1 | TC：每模块独立 ModuleNode + 共享 graph；Pass1 先注册全部声明，Pass2 再查函数体；去 `tc_module_of_line` 行映射依赖 | 单模块程序照常编译 |
| M2 | TC：import 可见性 + pub 导出检查 + 歧义报错 | 新增 `ns_amb`/`ns_priv` 用例 |
| M3 | codegen：模块前缀 IR 名 + `cg_global_lookup` 带模块键；`cg_gen_program_multi` 接线；extern 豁免 | a.c/b.c 共存用例通过 |
| M4 | 端到端：同名共存 + `import a.c as ac; import b.c as bc;` 别名 | 专项用例 PASS |
| M5 | 全量自举 fixed-point（stage2.ll==stage3.ll）+ 57/57 + LSP 4/4 | 收口 |

> 注：M3 已实现 codegen 多模块地基（链接 API 验证中），后续 M0/M1/M2 改造加载与 TC 后即可接线 `cg_gen_program_multi`。

### 2026-08-06 补充：严格态 import 规则最终对齐 Java（用户明确要求"完全按 Java"）
- **路径模型**：点号=目录，`a.b.c` → `a/b/c.shadow`，取消"首段=模块名"（Go 模型，已推翻）。
- **循环 import**：严格态**放开**（Java 允许 a↔b 互引；禁的是循环*继承*）。兼容态保留 `circular import detected`。
- **默认可见性**：未修饰符号 = **同目录（同包）可见**（package-private），跨包不可见。符号按完整模块路径
  mangle（文件级唯一），同目录多文件同名私有符号不冲突。
- **通配符不递归**：`import a.b.*` 仅引入 `a/b/` 直接子文件，不匹配 `a/b/c/` 子包（Java 规则）。
- **dsb 强制**：`dsb <name>;` 的 `<name>` 必须 == 文件 basename（对标 Java class 名==文件名）；仅严格态强制。
- **FQN 无需 import**：**不采用**（用户明确不需要）。`a.b.c.d()` 仍需先 `import a.b.c;`。
- 符号可见性其余 4 条（不传递 / 私有跨模块不可见 / 未 import 不能用 pub 简名 / 同名跨命名空间共存）此前已实现。

