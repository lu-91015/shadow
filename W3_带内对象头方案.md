# W3 带内对象头（In-Band Object Header）完整设计方案

> 状态：**P1–P3b 已实施并全量验证**（2026-08-30）
> P1 双写 0ab44b5 → P2 读路径切头 b5c26ec → P3 GCMetaMap 退役 ccd23c9 →
> P3b 整段重置回收 6d39011；四向放大版对 Go 比值大幅收敛（见 §6 迁移路线）。
> 前置：容器对象全部纯 C POD 化（50cb78f / ec1d329）。
>
> 目标读者：shadow-0.5 维护者。所有改动集中在 `rt/linux/runtime_for_selfhost.cpp`
> （双平台唯一 runtime 事实来源），**编译器侧（src/）零改动**为设计硬约束。

---

## 1. 背景与动机

### 1.1 现状：外部元数据哈希表（GCMetaMap）

每个 GC 对象的元数据存放在外部的 `GCMetaMap`（开放寻址哈希表，按对象指针寻址）：

```cpp
struct Entry {
    void* key;      // 对象指针
    GCMeta meta;    // ~56B：marked/visited/kind/owned/size/req/gen/survived/finalizer×2/finalized
    int32_t next, prev; // 活条目链表
};  // 每槽 ~80B
```

所有 GC 路径都对它做哈希查找：

| 路径 | 操作 | 热度 |
|---|---|---|
| `gc_trace_child` | `find(child)` + visited CAS | **最热**：标记期每条对象内指针一次 |
| `shadow_gc_write_barrier` | `find(parent)` + `find(child)` | 高：每次老→幼引用写入 |
| `shadow_gc_alloc` 慢路径 | 锁 + `insert`（80B/条） | 中：分配 |
| `rt_alloc_cap` | `g_pend` 线性扫 + 锁 + `find` | 高：字符串就地拼接热循环 |
| `gc_sweep_dead` | `for_each`（80B/条随机访存） | 中 |
| promotion/pacing | `for_each` 统计 | 低（每轮一次） |

已知代价（代码注释与历史事故均有记录）：

1. **缓存不友好**：80B/槽、指针哈希分散；注释实测「524288 槽 × 80B = 42MB 远超 L3」，
   str_reverse 曾因此每轮 GC 退化 100×，后续靠墓碑+活链表+收缩打补丁。
2. **分配慢路径重**：锁 + 哈希插入；为规避它引入了 `g_pend` 攒批（≤64），
   又导致 `rt_alloc_cap` 等需要「先扫攒批再查表」的双路径。
3. **元数据内存放大**：每对象 80B 元数据 vs 对象本体常常只有 16–64B。
4. **清扫靠哈希遍历**：无法按内存顺序线性回收，也无法整块归还内存。

### 1.2 目标

把每对象热路径元数据压缩成 **16B 带内头**（紧贴对象本体前置），对象身份判定改为
**分段堆区间检查**，从而：

- `trace_child`：哈希查找 → 区间二分 + 读 16B 头（每边预计省 10–15ns）；
- `alloc`：慢路径去掉锁+哈希插入；`g_pend` 攒批机制整体退役；
- `sweep`：80B 随机遍历 → 16B 步长线性遍历 + **整段回收**（对分配悬崖基准是结构性改善）；
- `rt_alloc_cap`：锁+查表 → 读头一个字段；
- 元数据内存 80B/对象 → 16B/对象。

### 1.3 非目标（本轮不做）

- 不改分代策略、GOGC pacing 模型、STW 协议、根集机制（帧锚点/named roots 原样保留）；
- 不做并发/增量标记；不做对象移动/压实；
- 不改编译器（codegen/runtime_lib 的 ABI 与对象布局假设全部不动）。

---

## 2. 头的布局

### 2.1 结构（16 字节，前置）

```c
// 对象头：位于载荷前 16 字节处（payload = (char*)obj_base + 16）。
// 「alive ⇔ epoch == g_gc_epoch」——分配即黑：分配时写入当前 epoch；
// 标记命中时 CAS 写入当前 epoch；清扫只认 epoch 相等。
// 无独立 marked 位 → 三色语义由单字承担，trace 的 CAS 无需掩码重试。
typedef struct ShadowHdr {
    uint32_t epoch;     // atomic：最后一次「分配或标记」的周期号
    uint16_t kind;      // RT_T_*：0 未分类保守 / 1 string / 2 C 布局数组
                        //          3 AnyBox / 4 rt_dict / 5 closure / ≥100 用户类型
    uint8_t  gen;       // 0=young 1=old（晋升语义不变）
    uint8_t  surv;      // minor 幸存计数（≥PROMOTION_THRESHOLD 晋升）
    uint32_t size;      // 对齐后载荷容量（保守扫描范围 = rt_alloc_cap 返回值）
    uint32_t req;       // 请求大小（GOGC pacing 记账，语义与现 meta.req 一致）
} ShadowHdr;            // 16 B
```

- **单字标记（取代 marked+visited 双字）**：现实现分离两字是为「分配即黑不被
  全量清 marked 破坏」；改用「存活判据 = epoch 相等」后，分配与标记写同一个字，
  天然免疫清理时序问题，且 `trace_child` 的 CAS 变成无掩码单字 CAS。
  `g_gc_epoch` 全局计数器语义不变（每次 collect 递增，tl_str_cache 的 ABA
  防护依赖它，原样保留）。epoch 回绕（2^32 轮）：回绕前插一轮「全量清扫」即可，
  工程上不可达，代码留 guard。
- **finalizer 不入头**：`finalizer_fn/data/finalized` 是冷路径极少数对象才用，
  入头会把头撑到 32B+。改放旁路小表（§3.5）。
- `owned` 字段消失：P3 之后所有 GC 对象都来自分段分配器，统一按头回收，
  「new 出来的要 delete」这一分叉随注册对象迁移而消失（§4.3）。

### 2.2 为什么是「前置头」而不是偏移 0 内置头

- codegen 与 runtime_lib **直接按固定偏移读写载荷**：shadow 层数组
  `[len:4][cap:4][elem_size:4][data@12]`、rt_dict `{buckets@0;n_buckets@8;size@12}`、
  AnyBox `[magic@0][tag@4][value@8]`、`gc_trace_object_children` 的逐类型偏移解析。
  头放偏移 0 会平移所有布局 → 编译器必须同步改，风险面失控。
- 前置头对载荷布局零侵入：`shadow_gc_alloc` 返回的仍是指向载荷的指针，
  全部现有 ABI 不变。头在 `(char*)payload - 16`。
- 对齐：分配粒度 16B，段基址 16B 对齐 → 载荷恒 16B 对齐（比现状
  「8B 对齐的 asize + malloc 16B」更严，无放宽风险）。

---

## 3. 堆结构：分段分配器

### 3.1 段（Segment）

```c
typedef struct GcSegment {
    char*  base;        // malloc 的整段内存（16B 对齐）
    size_t cap;         // 段容量（默认 1MB，env SHADOW_GC_SEG_CAP 可调）
    size_t used;        // bump 指针（含头）
    size_t live_bytes;  // sweep 维护：存活载荷字节数（整段回收判据）
} GcSegment;

static GcSegment** g_segments;   // 按 base 升序
static size_t      g_segment_n;  // 段数 = 堆大小/1MB，通常几十个
```

- **bump 分配**：`off = used; used += 16 + asize`；越界 → 新段（全局锁，罕见）。
- **对象身份判定**（替代 `g_gc_meta.find` 的唯一入口）：

```c
// 命中返回头指针；候选指针非法/非堆对象返回 nullptr。绝不裸读候选指针内存。
static inline ShadowHdr* gc_header_of(void* p) {
    // g_segments 按 base 有序 → 二分（段数少时线性更快，实现按段数自适应）
    // 条件：base <= p-16 && p <= base+used && ((char*)p - base) % 16 == 0
    //       && off >= 16
}
```

  保守扫描/栈槽里读出的任意候选先过区间+对齐检查，才允许读 16B 头 —— 与现状
  「哈希 miss 即忽略」语义一致，无野读风险。

### 3.2 清扫 = 线性遍历 + 整段回收

```
for each segment:
    live_bytes = 0
    for (off = 16; off < used; off += 16 + hdr(off).size):
        h = hdr at off-16
        if h->epoch == g_gc_epoch:          // 存活
            promotion 记账（gen/surv，语义同现状）
            live += h->req;  live_bytes += h->size
        else:                                // 死亡
            kind 分派终结逻辑（见 3.3）
            heap_bytes -= h->req
            若本段不整体回收 → fl_push(off-16, size)   // 复用槽位（含头）
    if live_bytes == 0 && used > 0 && 段非当前 bump 段:
        整段归还：free(base)，从注册表摘除
        （该段死对象不再 fl_push —— freelist 不得引用已归还内存）
```

- 16B 步长顺序访存，缓存友好；对比现状 80B 随机槽位是数量级差异。
- **整段回收**是新增能力：分配密集期成段产生、死亡期成段归还，直接缓解
  四向放大版暴露的「堆规模增大 → 清扫/分配超线性」悬崖。
- 空闲链表现有 14 个尺寸类（8–65536）保留，push/pop 的单位变为「含头槽位」，
  pop 出来重写的就是头本身；>65536 仍走原路径。
- **大对象**（asize > 段容量/4 或 > 256KB）：单独 malloc + 头，挂大对象链，
  清扫时单独遍历；其余规则一致。

### 3.3 死亡对象的终结分派（替代「owned 决定 free/delete」）

现状 sweep 按 `kind==1 → delete ShadowArray`、`kind==3 && !owned → delete AnyBox`、
其余 free/fl。P3 后所有对象都是分段槽位，分派改为「回收前是否需要释放内部资源」：

| kind | 回收动作 |
|---|---|
| 1（string）/ 2（C 数组）/ 5（closure）/ ≥100（用户类型） | 直接复用/归还（纯字节） |
| 3（AnyBox） | 直接复用（16B 纯字节，P3 后不再有 new 版本） |
| 4（rt_dict） | 桶/节点是 GC 对象 → 同样直接复用 |
| 未来：RFS 容器（若纳入 GC，见 §6） | 先调对应 release（如 `sdict_release`）再复用 |

### 3.4 多线程分配

- 每线程一个当前段（`ThreadGCState::cur_seg`），bump 只碰自己的段，无锁；
  申请新段、段注册表变更走全局锁（频率 ≈ 堆增长/1MB）。
- STW 协议不变：collect 期间所有 mutator 停在安全点，bump 指针冻结，
  与现状「等锁置 stw_state=2」契约等价。
- `fl_pop` 多 mutator 禁用、单 mutator 启用的现状策略原样保留。

### 3.5 Finalizer 旁路表

```c
// 只有调用过 shadow_gc_set_finalizer 的对象才进表（当前代码中极少）
static std::unordered_map<void*, FinalizerRec> g_finalizers; // 或同风格开放寻址小表
```

- 清扫遇死对象先查旁路表（只对「可能注册过 finalizer 的 sweep」查，死对象数
  通常远小于存活数，且表极小）；命中 → 执行/复活语义与现状完全一致
  （执行后置 `epoch = g_gc_epoch` 复活，下一轮再判）。
- 表项在对象真正回收时擦除。

---

## 4. 各路径改动对照

| 路径 | 现状 | 方案后 |
|---|---|---|
| `shadow_gc_alloc` | malloc/fl_pop → 攒批 or 锁+哈希 insert | bump/fl_pop → 写 16B 头（epoch/kind/size/req）→ heap_bytes+=。**无哈希、无攒批、单 mutator 无锁** |
| `gc_trace_child` | `g_gc_meta.find` → visited CAS → marked=1 | `gc_header_of` → epoch 单字 CAS |
| `gc_trace_object_children` | 读 `meta.kind/size` | 读 `hdr->kind/size`（签名改为传 `ShadowHdr*`） |
| `shadow_gc_write_barrier` | 两次 find + 锁 | 两次头读取（gen 在头里）；锁可降级为 stw 检查 |
| `rt_alloc_cap` | 扫 g_pend + 锁 + find | `gc_header_of` → `hdr->size`（无锁） |
| `gc_sweep_dead` | for_each 哈希 → erase → free/delete | 分段线性遍历 → 复用/整段归还（§3.2） |
| promotion/pacing | for_each 统计 marked/visited | sweep 内顺带完成（同一次遍历） |
| `shadow_free`（显式释放） | `shadow_gc_forget`（meta.erase）+ free | `gc_header_of` 命中 → epoch 写死值（0）+ 槽位复用；未命中 → 走现有 type_tag 容器分派（不变） |
| `shadow_gc_register` | 登记 new 对象（owned=0） | P2 起新对象全部走分配器；API 保留，降为「外来对象旁路表」（兼容 codegen 的 `rt_gc_register` 映射，实际调用方预期为零） |
| `shadow_gc_register_type` / 类型位图表 | 外部类型表 | **不变**（与每对象元数据正交） |
| GCWATCH / referrers / meta_contains | for_each | 分段遍历 / `gc_header_of` |
| `g_pend` / `g_pend_bytes` / `gc_flush_pending_locked` | 攒批避免哈希插入 | **整体删除**（头随分配写入，无延迟登记） |
| `GCMetaMap`（80B 槽、墓碑、活链表、收缩） | 核心结构 | **P3 删除** |

---

## 5. 与现有机制的兼容性核对

| 机制 | 结论 |
|---|---|
| codegen ABI（`shadow_gc_alloc(size,kind)` 返回载荷指针） | 不变 |
| shadow 层数组/rt_dict/AnyBox 载荷布局与直接偏移访问 | 不变（头在载荷之前） |
| 帧锚点根扫描（TLS 直写）、named roots、range roots、conservative regions | 不变（根集与对象元数据正交） |
| `g_gc_epoch` 语义（tl_str_cache ABA 防护） | 不变，仍是唯一全局周期计数 |
| GOGC pacing（live=visited==epoch 的 req 和；防「分配即黑残留」正反馈） | 语义逐字保留：存活判据换成 `epoch==current`，live 统计口径不变 |
| ex_gc_longrun（精确模式禁保守全堆扫） | 不变 |
| SHADOW_GC_CONSERVATIVE=1 对拍模式 | 候选判定从哈希 miss 换成区间检查，语义一致 |
| stress 模式（rt_gc_stress_n 每 N 次分配强制 collect） | 不变（触发检查留在 alloc 入口） |
| Windows `rt_gc.c` / `rt_core.c` | 冻结（历史参照实现；双平台事实来源为 cpp，c62fd5b 已统一） |
| 自举 fixed-point | 本方案不改 src/，stage*.ll 不受影响；每步仍走 FIXEDPOINT_OK 验收 |

---

## 6. 迁移路线（5 步，每步独立提交、独立验证）

> 原则：双轨过渡、读路径可回退、每步跑满验证矩阵（§7）。任何一步回归都能
> 单步 git revert，不牵连后续。

### P1 — 分段分配器 + 头写入（双写期）

- 新增 `ShadowHdr` / `GcSegment` / `gc_header_of` / 段注册表。
- `shadow_gc_alloc` 改走段分配并写头；**同时保留** `GCMetaMap` 插入（双写）。
- 读路径全部仍走 meta；仅 `rt_alloc_cap` 改为「读头，SHADOW_GC_VERIFY=1 时
  与 meta 比对，不一致即 abort」——提前暴露布局/记账错误。
- 验收：全量双平台 + ex_gc_longrun/spawn + VERIFY 对拍零差异。

### P2 — 读路径切到头（meta 降级为注册对象表）

- `gc_trace_child` / `write_barrier` / `trace_object_children` / sweep 的
  存活判定全部改读头（`epoch==current`）；`marked` 字段退役。
- `GCMetaMap` 只保留 `owned=0` 注册对象与 finalizer 元数据。
- 攒批（g_pend）删除；`rt_alloc_cap` 去锁。
- 验收同上 + SHADOW_GC_LOG/GCWATCH 抽样对比新旧行为。

### P3 — 注册对象迁移，GCMetaMap 退役

- 现存 `new + shadow_gc_register` 位点（ShadowArray 2 处、AnyBox 5 处）改为
  `shadow_gc_alloc(sizeof(...), kind)` + placement 构造；`owned` 概念消失。
- finalizer 移入旁路表；`GCMetaMap`、墓碑/活链表/收缩代码删除。
- sweep 切到纯分段遍历 + 整段回收；`shadow_free` 走头判据。
- 验收全矩阵 + 四向放大版对比（验证整段回收对规模悬崖的效果）。

### P4 — 性能硬化（按需）

- per-thread 段、尺寸类调优、段收缩阈值、大对象路径打磨；
- 若四向数据支持：评估「年轻代专用段 → minor 整段回收」。

### P5 — 清理与文档

- 删死代码与过期注释（含 GCWATCH 里 kind 语义错配的旧分支）；
- 同步 `开发原则.md` §5（GC 系统）与 §6（运行时）。

---

## 7. 验证矩阵（每步必跑）

1. `bash tools/build.sh --boot`：FIXEDPOINT_OK stage2==stage3。
2. `bash tools/build.sh --linux`：Route A link_rc=0 + matmul e2e。
3. `bash tools/test_basic.sh`：Win 953/0/29/982、Linux 953/0/CRASH0/2/955。
4. `bash tools/bench_short.sh`：六例规范值全对，不低于基线（2026-08-30：
   Win 0.91–1.12× go，Linux 0.33–0.90× go）。
5. GC 专项：`ex_gc_longrun`（堆谷底 ratio<1.10）、`ex_gc_spawn`（8 worker 并发）、
   `str_reverse`、`matmul`；`SHADOW_GC_STRESS_N=1`（每次分配触发 collect）。
6. P1 双写期：`SHADOW_GC_VERIFY=1` 头/表一致性对拍零差异。
7. `bash tools/verify_build.sh --update`：产物指纹随提交刷新。

---

## 8. 风险与缓解

| 风险 | 缓解 |
|---|---|
| 身份误判：保守候选穿过区间+对齐检查 | 16B 对齐已滤掉绝大多数；调试期双轨对拍（P1）；可选在头里留 4 bit 校验位 |
| 段注册表并发读写 | 注册表只在「申请新段/整段回收」时变更，两者都在全局锁内；读侧二分只读 |
| epoch 回绕 | 2^32 轮不可达；留 guard：回绕前强制一轮全清 |
| 多线程分配回归（ex_gc_spawn） | per-thread 段 + STW 不变；该用例为每步必跑门禁 |
| 整段回收误伤（freelist 悬引用） | 待回收段的死对象不进 freelist（§3.2 规则） |
| finalizer 复活时序变化 | 复活=写当前 epoch，与现状「多活一轮」逐字等价 |
| 任何步骤回归 | 双轨读路径可回退；git 每步独立提交可单步 revert |

---

## 9. 预期收益汇总

- **标记**：每条对象内指针省一次哈希探测（≈10–15ns/边，指针密集对象线性放大）。
- **分配**：慢路径去锁去哈希；攒批机制消失，代码路径减半。
- **清扫**：线性访存 + 整段归还 → 直接针对四向放大版的规模化悬崖。
- **字符串拼接**：`rt_alloc_cap` 去锁去查表（str_reverse 类热循环直接受益）。
- **元数据内存**：80B/对象 → 16B/对象；「42MB 元数据超 L3」问题不复存在。
