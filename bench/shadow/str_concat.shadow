// 基准测试：字符串拼接（动态字符串构建）
dsb str_concat;

kimo main() -> int {
    let s = "";
    let i = 0;
    while (i < 20000) {
        s = s + "abc";
        i = i + 1;
    }
    println(len(s));
    return 0;
}
