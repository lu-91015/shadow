// 基准测试：动态数组 push（内存分配 + 扩容）
dsb array_push;

kimo main() -> int {
    let a = [];
    let i = 0;
    while (i < 1000000) {
        a = array_push(a, i);
        i = i + 1;
    }
    println(len(a));
    println(a[999999]);
    return 0;
}
