// 基准测试：递归斐波那契（函数调用开销）
dsb fib;

kimo fib(n: int) -> int {
    if (n < 2) { return n; }
    return fib(n - 1) + fib(n - 2);
}

kimo main() -> int {
    let r = fib(32);
    println(r);
    return 0;
}
